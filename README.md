# Agent Traffic Control

A coordination toolkit of 104 [Claude Code](https://claude.com/claude-code) skills — plus one installable hook — for **running multiple parallel sessions against the same repo without collisions, stranded work, or rebase loops**: issue-pickup claim protocol, worktree & session-isolation pitfalls, parallel-PR conflict recovery, subagent-integrity edge cases, and the squash/merge mechanics that bite when multiple PRs converge on the same branch.

[![license](https://img.shields.io/github/license/wan-huiyan/agent-traffic-control)](LICENSE)
[![last commit](https://img.shields.io/github/last-commit/wan-huiyan/agent-traffic-control)](https://github.com/wan-huiyan/agent-traffic-control/commits)
[![Claude Code](https://img.shields.io/badge/Claude_Code-plugin-orange)](https://claude.com/claude-code)

## When to reach for this toolkit

- You routinely run **2+ Claude Code sessions in parallel** against the same repo (worktrees, separate terminals, scheduled overnight loops, or sandbox + main).
- You've seen **two sessions independently start work on the same issue**, **PRs collide on rebase**, **subagent reports look complete but the PR never merged**, **a worktree's index goes corrupt right after committing in a sibling**, or **GitHub squash-merge auto-closes only one issue out of three you listed in the PR body**.
- You want **upstream prevention** at the issue-pickup boundary plus **downstream recovery** for the failure modes that still slip through.

## Installation

```bash
# Add the marketplace
/plugin marketplace add wan-huiyan/agent-traffic-control

# Install the plugin — one shot, gets all 104 skills
/plugin install agent-traffic-control@wan-huiyan-agent-traffic-control
```

Installing the plugin gets you the skills. The `resume-gate` **hook is not installed by this** and never fires until you run its installer by hand — see [The hook](#the-hook--resume-gate) below.

This is a single multi-skill plugin (modeled on `superpowers`), not a marketplace of individual plugins. One install gets you the full `before / during / after / orchestrator-aware / merge-mechanics` arc; you can't pick-and-choose per-skill via `/plugin install`. If you only want one or two of these skills, copy them directly into `~/.claude/skills/<skill-name>/` instead.

## The six buckets

The 104 skills split into a **before / during / after / orchestrator-aware / merge-mechanics / workflow-orchestration** arc:

### A. Pickup / claim coordination — *prevention*

Before any code is written, claim the issue so sibling sessions detect it and skip.

| Skill | Role |
|---|---|
| [**gh-issue-claim-coordination**](plugins/agent-traffic-control/skills/gh-issue-claim-coordination/) | 60-second protocol: preflight (`gh issue view --json assignees,labels,updatedAt`) + atomic claim (`--add-assignee @me --add-label wip`) + 24h stale-claim sweep + label-drop on PR merge. Self-heals: idempotent `gh label create` runs every pickup. |
| [**session-handoff-number-collision-with-unmerged-sibling**](plugins/agent-traffic-control/skills/session-handoff-number-collision-with-unmerged-sibling/) | Two sibling sessions both pick the same next handoff number from `docs/handoffs/` because each only sees what's merged on its branch — detect and renumber before push. |
| [**session-handoff-detect-prior-orphan-pr**](plugins/agent-traffic-control/skills/session-handoff-detect-prior-orphan-pr/) | Pre-flight detect a prior incomplete handoff run's branch/PR/worktree before starting, so you don't open a duplicate PR for work already in flight. |
| [**executed-prompt-not-retired-sibling-redoes-it**](plugins/agent-traffic-control/skills/executed-prompt-not-retired-sibling-redoes-it/) | A brief and the index that lists it are shared state — retire an executed prompt on BOTH surfaces (and register a new one), or a sibling redoes work that already merged, or never sees the file at all. |
| [**parallel-session-coedit-via-source-mtime-and-idempotent-rebuild**](plugins/agent-traffic-control/skills/parallel-session-coedit-via-source-mtime-and-idempotent-rebuild/) | Safely co-edit a deliverable while another live session is actively editing the same file — coordinate via source mtime + an idempotent rebuild instead of clobbering. |
| [**verify-live-prod-before-shipping-superseded-fix**](plugins/agent-traffic-control/skills/verify-live-prod-before-shipping-superseded-fix/) | A parallel session already shipped a (often better) fix for the same live-prod artifact while you built yours — verify current prod state before redoing or overwriting. Also covers the case where you built NOTHING and the deliverable is an ASSESSMENT ("do not deploy"): pin the base SHA at the start and diff it against main before publishing the verdict. |
| [**shared-file-redesign-parallel-author-serial-integrate**](plugins/agent-traffic-control/skills/shared-file-redesign-parallel-author-serial-integrate/) | Parallelize a multi-slice redesign whose slices ALL edit one hot file (template, central view, shared CSS) — structure the handoff so authors work in parallel but integrate serially. |
| [**shared-mutable-index-rmw-race-use-marker-blob-per-item**](plugins/agent-traffic-control/skills/shared-mutable-index-rmw-race-use-marker-blob-per-item/) | Designing a shared "who's active / recent items" index with concurrent producers — dodge the read-modify-write race with a marker-blob-per-item pattern. |
| [**recover-killed-session-from-transcript-and-worktree**](plugins/agent-traffic-control/skills/recover-killed-session-from-transcript-and-worktree/) | Recover a prior session's work, plan, and failure-cause from its transcript + worktree after it crashed or was killed mid-task. Its automatic trigger is the [`resume-gate`](plugins/agent-traffic-control/hooks/resume-gate/) hook below. |

See also: `superpowers:dispatching-parallel-agents` — when to fan out vs. not (not bundled here; ships with the official `superpowers` plugin).

## The hook — `resume-gate`

Every skill in this toolkit fires because a human, or the model, notices something and reaches for
it. One failure mode gives nobody anything to notice: **a subagent killed mid-run leaves its partial
output sitting in the parent's transcript, and a later session resuming that branch reads it as a
finished, reviewed result and ships it.**

[**resume-gate**](plugins/agent-traffic-control/hooks/resume-gate/) is the trigger for that case —
a `SessionStart:resume` detector plus a `PreToolUse` gate:

- On resume it names every subagent whose work the interrupted session never assessed, under two
  signatures: the harness's own partial-output notice for a **synchronous** subagent, and a
  non-`completed` task-notification status for an **asynchronous** one. The async half is the one
  that matters — 81 of 181 measured Agent calls return only `Async agent launched successfully`, so
  no partial-output notice ever reaches the parent no matter what happens to the subagent.
- Until those are re-verified, all seven ship conditions — `git push`, `git -C <path> push`,
  `gh pr merge`, `gh pr create`, `gh pr edit` (which covers `--add-label auto-deploy`, a deploy with
  no push or merge verb in the command at all), `gh api` (a REST-API merge), `gh release create` —
  plus the configured publish tools get `permissionDecision: "ask"`. A human keypress, not an
  auto-deny the model can retry past. **Not commits.**
- It warns **once per interruption**, not once per resume: the detector writes a mark into the
  transcript carrying what it found, and clears that mark when nothing is outstanding.

**It is opt-in and installed by hand** — `python3 install.py` after copying it to a stable
directory. It is deliberately *not* registered as a plugin-native hook, so installing or updating
this plugin never changes how your sessions behave until you choose to run the installer. The
installer **refuses to run from a plugin cache, a worktree, or `/tmp`**, because the hook config
stores an absolute path and a `PreToolUse` hook whose script has vanished exits 2 — which blocks the
tool call, so every push on the machine fails with nothing naming the cause. That happened once
already; see [DESIGN.md](plugins/agent-traffic-control/hooks/resume-gate/DESIGN.md), which also
records the two earlier designs that were built, measured and rejected.

94 tests, plus a committed mutation harness (`tests/mutation_check.py`, 38 mutations) so its own
test-quality claim can be re-run rather than believed. Fixtures are generated, not captured —
`make_fixtures.py` builds them deterministically and a test regenerates and compares byte for byte.
Both run in CI.

### B. Worktree & session isolation pitfalls

The cheap-isolation primitive (`git worktree`) has surprising failure modes when paired with parallel sessions, async post-commit hooks, and sibling subagents.

| Skill | Role |
|---|---|
| [**using-git-worktrees**](plugins/agent-traffic-control/skills/using-git-worktrees/) | *Reference-only since v1.18.0* — directory-selection priority and the ignore check, written out. Its description shared its first 118 characters verbatim with `superpowers:using-git-worktrees`, which is installed alongside it, so the listing paid twice for one trigger surface; `git-worktree` below covers the same moment with a script. |
| [**git-worktree**](plugins/agent-traffic-control/skills/git-worktree/) | Create, list, switch and clean up worktrees via the bundled `worktree-manager.sh` — copies `.env` in, gitignores the directory, trusts mise/direnv. |
| [**gh-pr-merge-worktree-checkout-trap**](plugins/agent-traffic-control/skills/gh-pr-merge-worktree-checkout-trap/) | Diagnose `branch is checked out elsewhere` errors when merging a PR while the same branch is checked out in another worktree. |
| [**worktree-index-corrupt-async-post-commit-hook**](plugins/agent-traffic-control/skills/worktree-index-corrupt-async-post-commit-hook/) | Fix `fatal: unable to read <sha>` errors that surface in worktree B after committing in worktree A — when an async post-commit hook (`&` / `nohup`) is in play. |
| [**worktree-historical-test-replay-missing-dirs**](plugins/agent-traffic-control/skills/worktree-historical-test-replay-missing-dirs/) | When a historical test replay fails because directories that exist in HEAD don't exist at the older commit. |
| [**worktree-outer-ls-mistaken-for-main-state**](plugins/agent-traffic-control/skills/worktree-outer-ls-mistaken-for-main-state/) | Confusing `.claude/worktrees/<name>` directory listing for the main checkout's state and acting on stale assumptions. |
| [**pr-hijack-via-stale-worktree-branch-ref**](plugins/agent-traffic-control/skills/pr-hijack-via-stale-worktree-branch-ref/) | A stale local worktree's branch ref overwrites a teammate's pushed commits when force-push or auto-update kicks in — verify upstream before pushing. |
| [**subagent-bash-cd-wrong-worktree**](plugins/agent-traffic-control/skills/subagent-bash-cd-wrong-worktree/) | Subagents inheriting the wrong CWD when dispatched from a worktree. |
| [**flask-debug-cross-worktree-edit-stale**](plugins/agent-traffic-control/skills/flask-debug-cross-worktree-edit-stale/) | Flask debug server reading stale code because the file you edited lives in a sibling worktree. |
| [**git-add-u-after-async-post-commit-hook**](plugins/agent-traffic-control/skills/git-add-u-after-async-post-commit-hook/) | `git add -u` racing with an async post-commit hook's writes — partial stages and surprising diffs. |
| [**git-rebase-stalls-async-post-commit-hook**](plugins/agent-traffic-control/skills/git-rebase-stalls-async-post-commit-hook/) | `git rebase` stalls or fails mid-replay because an async post-commit hook is holding a lock or writing to the index. |
| [**git-amend-hits-async-post-commit-hook-commit**](plugins/agent-traffic-control/skills/git-amend-hits-async-post-commit-hook-commit/) | `git commit --amend` folds your change into a background post-commit-hook commit instead of your own feature commit. |
| [**pre-dispatch-agent-isolation-parameter-not-prompt**](plugins/agent-traffic-control/skills/pre-dispatch-agent-isolation-parameter-not-prompt/) | Before fanning out agents that will commit, confirm the dispatch CALL carries `isolation: "worktree"` — a prompt sentence telling each agent it is isolated creates nothing — and make every agent print its toplevel + branch as its first bash call. Prevention only — the three recovery skills it links are what this looks like once it has already happened. |
| [**concurrent-session-checkout-clobbers-shared-worktree**](plugins/agent-traffic-control/skills/concurrent-session-checkout-clobbers-shared-worktree/) | A second session's `git checkout` flips the branch under your shared working tree, clobbering uncommitted work — detect and recover via an isolated worktree. Every trigger is a symptom, so it fires only once damage exists; prevention is the row above. |
| [**cross-worktree-spec-handoff-via-checkout-paths**](plugins/agent-traffic-control/skills/cross-worktree-spec-handoff-via-checkout-paths/) | Pass specs/handoff prompts between two parallel sessions on different worktree branches without round-tripping through main. |
| [**main-bash-cwd-persists-nested-worktree**](plugins/agent-traffic-control/skills/main-bash-cwd-persists-nested-worktree/) | The main agent's Bash cwd persists across calls, so orchestrated worktree creation lands at the wrong (nested) path. |
| [**multi-worktree-file-url-stale-content**](plugins/agent-traffic-control/skills/multi-worktree-file-url-stale-content/) | A `file://` bookmark serves the targeted worktree branch's content, not main, after a merge — stale-content confusion across checkouts. |
| [**git-stash-pop-pulls-unrelated-stash**](plugins/agent-traffic-control/skills/git-stash-pop-pulls-unrelated-stash/) | The stash stack is global across branches and worktrees; a reflexive `stash pop` can pull a sibling worktree's stash. |
| [**claude-code-projects-jsonl-worktree-fanout**](plugins/agent-traffic-control/skills/claude-code-projects-jsonl-worktree-fanout/) | Session JSONLs fan out into worktree-namespaced project dirs — grepping only the canonical dir misses worktree-run sessions. |
| [**deploy-from-stale-worktree-silent-rollback**](plugins/agent-traffic-control/skills/deploy-from-stale-worktree-silent-rollback/) | Deploying from a worktree whose HEAD predates merged PRs silently rolls back prod — build context is the filesystem, not the git ref. |
| [**async-doc-hook-autodocs-worktree-locks-branch-checkout**](plugins/agent-traffic-control/skills/async-doc-hook-autodocs-worktree-locks-branch-checkout/) | `git checkout <branch>` fails "already used by worktree" because an async autodocs hook is holding a worktree on that branch — find and release it. |
| [**git-auto-maintenance-recurring-worktree-index-lock**](plugins/agent-traffic-control/skills/git-auto-maintenance-recurring-worktree-index-lock/) | A recurring `index.lock: File exists` that returns after you `rm` it, because your own commands keep spawning `git maintenance run` in a busy multi-worktree repo. |
| [**harness-read-write-base-repo-path-in-worktree-stale-tree**](plugins/agent-traffic-control/skills/harness-read-write-base-repo-path-in-worktree-stale-tree/) | In a worktree session, Read/Write/Edit calls using a BASE-repo absolute path (missing the `.claude/worktrees/<name>/` segment) silently operate on the wrong tree. |
| [**worktree-write-abs-path-lands-in-parent-checkout**](plugins/agent-traffic-control/skills/worktree-write-abs-path-lands-in-parent-checkout/) | A Write/Edit whose absolute path points at the main-repo root silently creates the file in the parent checkout's working tree — on whatever branch it has out. |
| [**worktree-stale-local-main-ref-inflates-pr-diff**](plugins/agent-traffic-control/skills/worktree-stale-local-main-ref-inflates-pr-diff/) | `git diff main...<branch>` reports far more files than you changed because local `main` is stale — a false "my PR reverts dozens of files" alarm; fetch first, and name `origin/main`. The complementary trap to the 2-dot one: 3-dot fixes the operator, not the ref. |
| [**shell-pinned-to-deleted-worktree-cwd-blocks-git**](plugins/agent-traffic-control/skills/shell-pinned-to-deleted-worktree-cwd-blocks-git/) | Every shell command fails `Unable to read current working directory` after the worktree you were in gets pruned — diagnose and recover the cwd. |
| [**verify-pytest-imports-worktree-not-primary-checkout**](plugins/agent-traffic-control/skills/verify-pytest-imports-worktree-not-primary-checkout/) | Running pytest from a worktree with an editable install (`pip install -e .`) made from the primary checkout imports the PRIMARY source, not your worktree's. |
| [**worktree-does-not-isolate-shared-installed-artefacts**](plugins/agent-traffic-control/skills/worktree-does-not-isolate-shared-installed-artefacts/) | A worktree isolates the SOURCE TREE only — a simulator install keyed by bundle id, DerivedData keyed by project path or a tool cache keyed by version string is shared by every worktree, so a run can exercise ANOTHER worktree's artefact and say nothing. |
| [**prove-test-failures-pre-existing-via-clean-worktree**](plugins/agent-traffic-control/skills/prove-test-failures-pre-existing-via-clean-worktree/) | Before panic-debugging (or waving off) test failures in files your diff never touched, prove they're pre-existing by replaying against a clean worktree. |

### C. Parallel-PR conflict recovery — *after collision*

When two sessions DO collide (or an old PR drifts behind a fast-moving main), these resolve the rebase cleanly.

| Skill | Role |
|---|---|
| [**parallel-pr-template-fork-duplicates-moved-section**](plugins/agent-traffic-control/skills/parallel-pr-template-fork-duplicates-moved-section/) | Two PRs both move a template section — merge produces duplicates. Detect + dedupe. |
| [**parallel-pr-scope-overlap-tiebreaker-delta-check**](plugins/agent-traffic-control/skills/parallel-pr-scope-overlap-tiebreaker-delta-check/) | Two PRs claim overlapping scope — pick the winner via delta-check rather than first-merged. |
| [**pr-conflict-from-mid-flight-merges**](plugins/agent-traffic-control/skills/pr-conflict-from-mid-flight-merges/) | Mid-flight merges of unrelated PRs creating cascading conflicts on your branch — the 6-step rebase recipe, plus why a stated COUNT is the one reconciliation where taking either side of the conflict is wrong. |
| [**stale-base-drops-rows-from-a-shared-ledger**](plugins/agent-traffic-control/skills/stale-base-drops-rows-from-a-shared-ledger/) | A branch with a stale TREE drops other sessions' rows from a shared hand-edited ledger (a tracker `data.js`, a registry, a CHANGELOG) with no file deletion, no conflict and an ordinary diffstat — so a file-level `--diff-filter=D` gate reports clean and the loss shows only as `-` lines in the content diff. A branch that is merely behind conflicts loudly instead. |
| [**stale-base-pr-silently-reverts-upstream-content**](plugins/agent-traffic-control/skills/stale-base-pr-silently-reverts-upstream-content/) | An old PR with a stale base silently reverts upstream content when merged — detect via base-vs-main diff before approving. |
| [**gha-pr-merge-ref-shows-upstream-changes**](plugins/agent-traffic-control/skills/gha-pr-merge-ref-shows-upstream-changes/) | The PR's merge ref shows upstream changes you didn't make — explain why and avoid panic-reverting. |
| [**synthetic-id-collision-rebase**](plugins/agent-traffic-control/skills/synthetic-id-collision-rebase/) | Two sessions both claimed the same numeric ID (tracker entry, ADR number, fixture row) — rename + propagate during rebase. |
| [**merge-conflict-generated-files**](plugins/agent-traffic-control/skills/merge-conflict-generated-files/) | Generated files (lock files, generated HTML, snapshot JSON) shouldn't be hand-merged — regenerate from the union of inputs. Also covers a derived VALUE inside a hand-authored file (a stated test count in a README): resolve to a placeholder, then measure the merged tree once. |
| [**gh-pr-create-orchestration-cwd-wrong-head**](plugins/agent-traffic-control/skills/gh-pr-create-orchestration-cwd-wrong-head/) | `gh pr create` opens the PR against the orchestration worktree's branch instead of the feature branch, spawning duplicate PRs. |
| [**gh-pr-merge-unstable-state-needs-auto-and-watch-branch-deletes**](plugins/agent-traffic-control/skills/gh-pr-merge-unstable-state-needs-auto-and-watch-branch-deletes/) | `MERGEABLE`+`UNSTABLE` failures masquerade as conflicts (pending CI); deleting the branch after a false conflict flips the PR to CLOSED. |
| [**git-diff-2dot-vs-3dot-merge-safety**](plugins/agent-traffic-control/skills/git-diff-2dot-vs-3dot-merge-safety/) | A 2-dot diff false-alarms "this PR deletes files on main" for a PR branched off an older commit — use 3-dot to check merge safety. The canonical statement of the operator rule; **model-invocable since v1.16.0**, so it can surface before you act on a 2-dot stat. |
| [**docs-branch-off-feature-branch-smuggles-code**](plugins/agent-traffic-control/skills/docs-branch-off-feature-branch-smuggles-code/) | A `docs(...)` PR branched off a feature branch (not main) silently ships the parent branch's code. |
| [**stacked-pr-review-per-base-diff-and-attach**](plugins/agent-traffic-control/skills/stacked-pr-review-per-base-diff-and-attach/) | Review a stack by diffing each PR against its own base (not all-vs-main) and attach reviews to the stack's base branch. |
| [**gh-pr-pickup-designated-branch-supersede**](plugins/agent-traffic-control/skills/gh-pr-pickup-designated-branch-supersede/) | Finish someone else's open WIP PR from a session locked to a different branch: base the designated branch on the PR head, supersede, close the draft — and dodge the upstream-hijack push trap. |
| [**large-redesign-parallel-branch-collision-audit**](plugins/agent-traffic-control/skills/large-redesign-parallel-branch-collision-audit/) | Before a large redesign (10+ PRs rewriting shared files), audit ALL unmerged branches for commits touching the same files, so parallel work doesn't silently collide. |
| [**pr-from-stale-branch-silently-reverts-newer-main-files**](plugins/agent-traffic-control/skills/pr-from-stale-branch-silently-reverts-newer-main-files/) | Merging a PR whose branch carries an OLD TREE silently DELETES files that landed on main after that tree was built — the commit records the deletion, so there is no conflict. Check deletions, not behindness: the damaging variant is not behind main at all. Covers both ends: the author's pre-merge check, and the VICTIM audit after someone else's merge — scope it to what you TOUCHED (not what you made), source every needle from the merged diff rather than from memory, and check the state AT the suspect commit rather than at `origin/main`. Whole-file sibling of `stale-base-pr-silently-reverts-upstream-content` (that one = line-level overlap). |

### D. Subagent integrity — *orchestrator-aware*

Subagents introduce their own coordination failure modes. These cover misattribution, incomplete reports, takeover during waits, silent phase compression, credit-stall recovery, stranded branch refs, watchdog stalls, source-completeness for verification subagents, and grep-verifying the dispatcher's own claims.

| Skill | Role |
|---|---|
| [**subagent-pre-existing-misattribution**](plugins/agent-traffic-control/skills/subagent-pre-existing-misattribution/) | Subagent claims credit for code that was already there before its dispatch. |
| [**subagent-reports-complete-but-pr-unmerged**](plugins/agent-traffic-control/skills/subagent-reports-complete-but-pr-unmerged/) | Subagent's "✅ done" doesn't mean the PR landed — verify merge state, not report state. |
| [**subagent-external-wait-orchestrator-takeover**](plugins/agent-traffic-control/skills/subagent-external-wait-orchestrator-takeover/) | When a subagent is blocked waiting on external state, the orchestrator can step in safely (or unsafely) — this skill covers the boundary. |
| [**multi-agent-skill-silent-phase-compression**](plugins/agent-traffic-control/skills/multi-agent-skill-silent-phase-compression/) | Multi-phase skills silently collapsing phases when the model decides the work was "small enough" — restoring phase boundaries. |
| [**multi-phase-skill-disk-reading-strategy**](plugins/agent-traffic-control/skills/multi-phase-skill-disk-reading-strategy/) | Late-pipeline subagents should read written files from disk, not pipe everything through the orchestrator's context. |
| [**credit-stall-mid-orchestration-revive-collision**](plugins/agent-traffic-control/skills/credit-stall-mid-orchestration-revive-collision/) | Reviving an orchestration after a credit stall — collision avoidance for in-flight work that may have continued. |
| [**handoff-prompt-stale-user-hint-newer-state**](plugins/agent-traffic-control/skills/handoff-prompt-stale-user-hint-newer-state/) | A handoff prompt carries a user hint that no longer matches current state — detect and re-confirm before acting. |
| [**subagent-driven-branch-ref-froze-stranded-commits**](plugins/agent-traffic-control/skills/subagent-driven-branch-ref-froze-stranded-commits/) | In subagent-per-task runs, committed work survives in the worktree HEAD but never reaches the pushed branch ref — the PR ships partial. |
| [**subagent-watchdog-stall-on-ui-template-track**](plugins/agent-traffic-control/skills/subagent-watchdog-stall-on-ui-template-track/) | A UI/template subagent is killed by the no-output watchdog (~600s silence); recover the uncommitted worktree changes and run inline rather than re-dispatching. |
| [**code-reviewer-subagent-no-bash-blocked-on-pr-diff**](plugins/agent-traffic-control/skills/code-reviewer-subagent-no-bash-blocked-on-pr-diff/) | Review subagents lacking Bash can't fetch PR diffs and return BLOCKED — pre-materialize the diff to a file before dispatch. |
| [**factcheck-subagent-needs-complete-sources**](plugins/agent-traffic-control/skills/factcheck-subagent-needs-complete-sources/) | Feeding a verification subagent an abridged source yields false-positive "unsupported" verdicts on the trimmed regions. |
| [**task-framing-claims-need-subagent-grep-verify**](plugins/agent-traffic-control/skills/task-framing-claims-need-subagent-grep-verify/) | Grant and require a dispatched subagent to grep-verify the dispatcher's task-framing claims about the codebase before acting. |
| [**pr-plan-bucket-triage-before-sizing**](plugins/agent-traffic-control/skills/pr-plan-bucket-triage-before-sizing/) | Run a subagent-per-bucket Phase-0 triage before writing detailed parallel-PR plans on an actively-shipped repo. |
| [**wip-branch-linter-revert-system-reminder-trap**](plugins/agent-traffic-control/skills/wip-branch-linter-revert-system-reminder-trap/) | A linter/automation system-reminder silently reverts deliberate WIP-branch constants during parallel work — don't accept the revert. |
| [**code-review-subagent-fabricates-specifics-to-inflate-severity**](plugins/agent-traffic-control/skills/code-review-subagent-fabricates-specifics-to-inflate-severity/) | A review subagent reports a HIGH/BLOCKING finding citing specific evidence (line numbers, call counts) that doesn't exist — verify the cited specifics before gating a merge; demote on fabrication. |
| [**db-access-review-subagent-needs-explicit-probe-budget**](plugins/agent-traffic-control/skills/db-access-review-subagent-needs-explicit-probe-budget/) | A review/verification subagent with live DB/cloud access needs an explicit tool-call + wall-time budget and a return-partial-on-exhaustion instruction, or it runs 20–40min and can lose its whole output. |
| [**dispatched-bash-agent-git-checkout-clobbers-uncommitted-edit**](plugins/agent-traffic-control/skills/dispatched-bash-agent-git-checkout-clobbers-uncommitted-edit/) | A verification/review subagent you dispatched runs `git checkout/restore/stash` and wipes your uncommitted edits — forbid or sandbox destructive git in dispatched agents. |
| [**isolate-subagent-verification-from-live-worktree**](plugins/agent-traffic-control/skills/isolate-subagent-verification-from-live-worktree/) | A gate-running agent dispatched into the worktree you are STILL editing measures the branch plus your uncommitted changes and reports the mixture as the branch's result — no clobber, no error, just numbers for a tree nobody will merge. Give it its own detached worktree at a pinned SHA. |
| [**subagent-read-stale-worktree-needs-head-pin**](plugins/agent-traffic-control/skills/subagent-read-stale-worktree-needs-head-pin/) | Read-only audit subagents silently return line numbers / "what exists" claims from the WRONG worktree in a many-worktree repo — pin them to the intended HEAD. |
| [**parallel-impl-agent-dies-mid-stream-verify-working-tree**](plugins/agent-traffic-control/skills/parallel-impl-agent-dies-mid-stream-verify-working-tree/) | A dispatched parallel impl subagent can die leaving ZERO output while the harness still reports "completed" — verify the working tree, don't trust the status. |
| [**parallel-subagent-fanout-rate-limit-recover-from-disk**](plugins/agent-traffic-control/skills/parallel-subagent-fanout-rate-limit-recover-from-disk/) | Large parallel subagent fan-outs hit a server-side rate limit AND the return status lies about what got written — recover the produced files from disk. |
| [**orchestrator-rule-too-strict-stalls-agent-silently**](plugins/agent-traffic-control/skills/orchestrator-rule-too-strict-stalls-agent-silently/) | A guard you wrote is scoped so wide its precondition never holds, so the agent waits correctly and ships nothing — no error, no red check, and a status that reads as work in progress. Scope guards to the object, not to global quiet, and ask each stalled agent whether one of YOUR rules is blocking it. |
| [**agent-refusal-with-evidence-beats-literal-compliance**](plugins/agent-traffic-control/skills/agent-refusal-with-evidence-beats-literal-compliance/) | Authorise an agent to refuse the letter of an instruction while meeting its intent — and require the measurement with the refusal, because evidence is the whole difference between a refusal you can bank and one you have to re-litigate. |

### E. Squash/merge mechanics — *the gotchas at PR-land time*

The squash/merge mechanics that bite when multiple PRs converge on the same branch.

| Skill | Role |
|---|---|
| [**gh-squash-merge-closes-only-one-issue**](plugins/agent-traffic-control/skills/gh-squash-merge-closes-only-one-issue/) | GitHub squash-merge auto-closes only ONE issue per PR — the rest stay OPEN even with `Closes #X, #Y, #Z`. Path A prevents (one keyword per issue: `Closes #X. Closes #Y.`); Path B recovers (find + close the orphans). |
| [**prep-pr-close-keyword-auto-closes-issue**](plugins/agent-traffic-control/skills/prep-pr-close-keyword-auto-closes-issue/) | A prep/scaffolding PR with a `Closes #X` keyword auto-closes the issue on merge — even though the feature isn't done. Use `Refs #X` for prep PRs; reserve `Closes` for the PR that actually ships the feature. |
| [**pr-followup-commit-stranded-after-squash**](plugins/agent-traffic-control/skills/pr-followup-commit-stranded-after-squash/) | Pushing a follow-up commit to a PR branch *after* the squash-merge lands the commit on a closed branch — never reaches main. Verify PR state before pushing; recover via fresh PR. |
| [**git-pull-after-squash-merge**](plugins/agent-traffic-control/skills/git-pull-after-squash-merge/) | After your PR squash-merges, `git pull` on the local feature branch leaves you in a confusing ahead+behind state. Reset to upstream main; don't try to reconcile. |
| [**stacked-pr-base-branch-deletion-auto-closes-dependent**](plugins/agent-traffic-control/skills/stacked-pr-base-branch-deletion-auto-closes-dependent/) | When a stacked PR's base branch is deleted (or auto-deleted by GitHub on merge), the dependent PR auto-closes — even if it had unmerged code. Re-target before the base disappears. |
| [**merged-pr-not-deployed-gate-label-missing**](plugins/agent-traffic-control/skills/merged-pr-not-deployed-gate-label-missing/) | A merged PR doesn't ship because a deploy-gate label is missing — verify gate labels on merge, not just merge status. |
| [**squash-merge-content-preservation-vs-ancestor-check**](plugins/agent-traffic-control/skills/squash-merge-content-preservation-vs-ancestor-check/) | `git merge-base --is-ancestor` always fails after a squash even when content is preserved verbatim — verify by content, not ancestry. |
| [**working-tree-edits-stranded-on-squash-merge**](plugins/agent-traffic-control/skills/working-tree-edits-stranded-on-squash-merge/) | Edit/Write don't stage; an unstaged fix is lost on squash-merge ("fix not on main") — stage before the squash lands. |
| [**safe-bulk-worktree-branch-cleanup**](plugins/agent-traffic-control/skills/safe-bulk-worktree-branch-cleanup/) | Bulk-clean stale worktrees/branches gating deletion on PR state, not ancestry (`git branch --merged` lies after a squash). |
| [**gh-pr-merge-squash-stdout-shows-sibling-files-as-created**](plugins/agent-traffic-control/skills/gh-pr-merge-squash-stdout-shows-sibling-files-as-created/) | `gh pr merge --squash` prints an alarming diffstat with `create mode` lines for sibling-PR files merged to main after your branch point — verify against the squash commit, don't panic-revert. |
| [**solo-repo-branch-protection-stable-gate-and-self-merge**](plugins/agent-traffic-control/skills/solo-repo-branch-protection-stable-gate-and-self-merge/) | Configure branch protection on a solo-maintained repo so red changes can't reach main, without locking yourself out — stable aggregation gate vs matrix check names, require-PR, self-merge with zero reviewers. |
| [**async-post-commit-hook-commit-orphaned-by-squash-merge**](plugins/agent-traffic-control/skills/async-post-commit-hook-commit-orphaned-by-squash-merge/) | In a repo with an async post-commit hook that makes its own follow-up commit, the hook's commit can be orphaned (never reach main) when the PR squash-merges. |
| [**merge-queue-thrash-stop-inflow-and-open-prs-as-drafts**](plugins/agent-traffic-control/skills/merge-queue-thrash-stop-inflow-and-open-prs-as-drafts/) | Main moves faster than the slowest CI leg, so every branch is BEHIND before its own run finishes and nothing lands with nothing red. Stop the inflow, land one at a time — and open every PR as a draft, which measured ~46s against ~16min and would have saved 23–48% of CI minutes over 8 days. |
| [**auto-merge-rearms-while-agent-live-kill-then-disarm-verify**](plugins/agent-traffic-control/skills/auto-merge-rearms-while-agent-live-kill-then-disarm-verify/) | Disarming auto-merge does not hold a PR whose agent is still running — arming is a step in the agent's brief, so it re-arms and merges. Kill the agent first, then disarm, then read the state back; or convert to draft, which GitHub enforces. |

> **Note:** `gh-squash-merge-closes-only-one-issue` also ships in [dashboard-audit-toolkit](https://github.com/wan-huiyan/dashboard-audit-toolkit) (where it surfaces as the operational gotcha when shipping audit fix-bundles at scale). Both wrap the same canonical skill.

### F. Workflow-tool & multi-agent orchestration mechanics — *building the fan-out itself*

Failure modes of the `Workflow` tool and multi-agent orchestration — schema/StructuredOutput agents, fan-out/pipeline plumbing, rate-limit waves, and the deploy-gate around it. (Useful when you drive parallel work through the `Workflow` tool rather than separate sessions.)

| Skill | Role |
|---|---|
| [**workflow-schema-agent-retry-cap-oversized-payload**](plugins/agent-traffic-control/skills/workflow-schema-agent-retry-cap-oversized-payload/) | A Workflow schema agent hits the retry cap on an oversized output — recover its work from the transcript instead of losing the whole run. |
| [**workflow-schema-agents-empty-loop-under-ratelimit**](plugins/agent-traffic-control/skills/workflow-schema-agents-empty-loop-under-ratelimit/) | Schema agents empty-loop StructuredOutput under a rate-limit storm — detect and back off instead of burning the fan-out. |
| [**workflow-standalone-schema-agent-crash-and-args-string**](plugins/agent-traffic-control/skills/workflow-standalone-schema-agent-crash-and-args-string/) | A standalone schema agent can crash the whole run; and `args` arrives as a JSON string — parse-guard it. |
| [**flatten-workflow-parallel-arrays-before-collecting**](plugins/agent-traffic-control/skills/flatten-workflow-parallel-arrays-before-collecting/) | A `pipeline()` stage that returns `parallel()` yields a bare array the collector silently drops — flatten inside the stage. |
| [**cjk-structured-llm-output-truncates-json-needs-2x-tokens**](plugins/agent-traffic-control/skills/cjk-structured-llm-output-truncates-json-needs-2x-tokens/) | Long CJK structured output truncates JSON at the token cap — budget ~2× tokens and salvage the partial. |
| [**inherited-scope-doc-names-may-not-exist**](plugins/agent-traffic-control/skills/inherited-scope-doc-names-may-not-exist/) | Probe the warehouse for the dataset paths, table names and column names a scope doc asserts, before a long or costly dispatch trusts them. |
| [**workflow-parallel-fanout-omits-sequential-phases**](plugins/agent-traffic-control/skills/workflow-parallel-fanout-omits-sequential-phases/) | Running a sequential multi-agent process as a parallel Workflow silently drops its sequential phases — model the dependency. |
| [**workflow-deterministic-compute-as-script-not-stall-prone-agent**](plugins/agent-traffic-control/skills/workflow-deterministic-compute-as-script-not-stall-prone-agent/) | Move deterministic compute to a direct script; don't lose it to a stalling agent turn. |
| [**workflow-run-deploy-gate-fork-pr-ref-name-escalation**](plugins/agent-traffic-control/skills/workflow-run-deploy-gate-fork-pr-ref-name-escalation/) | A `workflow_run` deploy gate that trusts the ref *name* is a fork-PR privilege-escalation hole — gate on the event, not the name. |
| [**opus-ratelimit-fanout-retry-on-sonnet-throttled-waves**](plugins/agent-traffic-control/skills/opus-ratelimit-fanout-retry-on-sonnet-throttled-waves/) | Opus rate-limit on a fan-out → retry on Sonnet, throttled into waves rather than one big burst. |
| [**design-subagent-with-plan-schema-executes-and-deploys-live-infra**](plugins/agent-traffic-control/skills/design-subagent-with-plan-schema-executes-and-deploys-live-infra/) | A "design/judge" subagent handed a plan schema can execute it and deploy live infrastructure — constrain it to proposing. |
| [**extract-workflow-markdown-from-output-json**](plugins/agent-traffic-control/skills/extract-workflow-markdown-from-output-json/) | Persist a Workflow's large markdown field by parsing its output JSON — never have an agent retype it. |
| [**workflow-agent-unreachable-mid-flight-check-output-not-brief**](plugins/agent-traffic-control/skills/workflow-agent-unreachable-mid-flight-check-output-not-brief/) | Whether an agent can be corrected mid-run is decided by the dispatch mechanism: a subagent has an address and keeps its context, a workflow phase has neither. Put anything whose premise may move in a subagent — and for what is already running, catch the stale rule at output review, at value level. |

## When NOT to reach for this toolkit

- **Single-session work** with no parallel-agent risk. The protocols are overhead with no benefit.
- **Cross-team coordination across separate repos** (real merge-queue territory) — out of scope.

## Version history

- **v1.30.0** (2026-08-17) — **Five new skills and two extended, all from one overnight fleet run, and every one of them is a case where the ORCHESTRATOR'S OWN INSTRUCTION was the defect.** Not a harness bug, not a git edge case — five separate ways a sentence written into a brief produced a failure the brief's author could not see. **(1) NEW — `merge-queue-thrash-stop-inflow-and-open-prs-as-drafts` (1.0.0).** Six pull requests sat at `BEHIND` with auto-merge armed and nothing landed: no conflicts, nothing red, every branch green at its last run and stale again before anyone could act. `BEHIND` is not `DIRTY`, so `pr-conflict-from-mid-flight-merges` does not fire and there is nothing to resolve. **The cause was the orchestrating session itself**, landing work continuously all night — when the interval between merges into `main` is shorter than the slowest required CI leg, the queue structurally cannot drain and auto-merge waits for a state no branch is ever in. Immediate fix: stop the inflow, land one at a time, newest CI first. **The structural fix is the invention worth the skill: open every pull request as a DRAFT** and promote it only when it is next to land. Measured on the repo it was learned on — a draft run **~46 seconds** against **~16 minutes** for a full one, and over **8 days** draft-by-default would have saved **23–48% of every CI minute**, because **36% of pull-request runs were merges of `main` into a branch carrying no new code at all**. Quote the 23–48% as a range measured on one repo; the transferable figure is the 36%, which is a property of how fast your main moves relative to CI and takes an afternoon to measure. It requires one line on the expensive job (`if: github.event.pull_request.draft == false`) — check the workflow file, not the intention, because a draft whose run still takes the full time saves nothing. **(2) NEW — `auto-merge-rearms-while-agent-live-kill-then-disarm-verify` (1.0.0).** Auto-merge was disarmed on a held pull request while its agent was still live; **it re-armed within minutes and merged. Twice.** The second time is the informative one: the mechanism is not timing, it is that **arming is a step in the agent's brief and the disarm was not addressed to the agent at all**. The general shape, which recurs well beyond auto-merge: changing a resource's state does not change the intent of a process that keeps re-asserting it — the same is true of a label, an assignee, a reviewer request. The order is **kill the agent first, then disarm, then read `autoMergeRequest` back, then read it back again five minutes later**, because the failure takes minutes and the verification everyone runs takes seconds. **The stronger form needs no ordering and no agent enumeration at all: `gh pr ready --undo`.** A draft cannot merge, whatever an agent does to it — a hold GitHub enforces rather than one agents are asked to respect. It inherits one trap from `gh-pr-merge-unstable-state-needs-auto-and-watch-branch-deletes`: a cleanup that deletes a head branch on branch-EXISTENCE will delete a held draft's branch and close the PR, so gate every delete on `mergedAt != null`. **(3) NEW — `orchestrator-rule-too-strict-stalls-agent-silently` (1.0.0).** A landing agent was told `gh pr update-branch` was safe only when **nothing was running repo-wide**. In a repo with several lanes pushing all night that is never true. **It obeyed, waited correctly, and produced nothing for an hour.** Nothing went red; the agent's status read as work in progress; CI was green because nothing had been pushed. It was found by a human asking why nothing had merged, which is not a monitoring strategy. **An over-strict rule is more expensive than a wrong one, because a wrong rule usually breaks something and tells you.** The correct guard is scoped per-branch — finished-green on the branch's current head, `mergeStateStatus: BEHIND`, and zero in-progress or queued runs **on that branch** — which is the same `gh run list` command minus one `--branch` flag, and that is exactly why it is easy to write and hard to spot. Two general forms ship with it: prefer **report** to **wait** in any unattended brief ("if the window has not opened in 10 minutes, stop and say which condition is unmet"), and **ask each stalled agent whether one of YOUR OWN rules is what is blocking it** — the agent can quote the clause, but will never volunteer it, because from inside the agent obeying an instruction is not a problem to report. **(4) NEW — `workflow-agent-unreachable-mid-flight-check-output-not-brief` (1.0.0).** A brief is frozen at dispatch, and **whether you can unfreeze it is a property of the dispatch mechanism, decided before you knew you would need to**: a directly-spawned subagent has an address and keeps its accumulated context when you message it; an agent running as a workflow phase has none, and correcting it means stopping the workflow, editing the script and resuming by run id, with completed phases replaying from cache but the agent's own context gone. The rule is therefore a dispatch-time one — **if the task's premise could move, do not put it in a workflow.** Observed cost: a release lane produced **eleven blockers** for a sibling lane running as a workflow, with no channel to it; every one went through the shared task list instead, with no confirmation that any had been read. The second half is for what is already running: **catch the stale rule on the way OUT, at output review**, because an inbound amendment reaches only agents that have not started. And the check must be **value-level, not text-level** — a reviewer corrected a set of data-retention periods mid-run and the authoring agent's brief still quoted the superseded wording; had it landed, the wrong values would have been inside a schema, where a grep for the old phrasing returns nothing and the values look like a deliberate design choice. This is deliberately scoped to what `overnight-multi-issue-implementation` (published in [wan-huiyan/overnight-workflows](https://github.com/wan-huiyan/overnight-workflows), not here) does **not** cover: that skill's *"Amend a running orchestration through a file on disk, not the script"* is the inbound half and is cited rather than restated. **(5) NEW — `agent-refusal-with-evidence-beats-literal-compliance` (1.0.0).** **Four times in one run an agent correctly declined what it was asked to do**, and not one was a failure. One built the suggested fix, measured that it converted a **silent safe refusal into a permanently red pipeline**, and **reverted it unpushed** — nothing to undo, because nothing had been pushed. One refused to record a facing direction that had been arrived at by inference rather than measurement, and was right: the value would have entered the record indistinguishable from measured ones. **Both halves are needed and shipping only the first is worse than shipping neither** — permission to satisfy the intent while refusing the letter, plus a requirement that the refusal arrive with what was built or measured, what it showed, and what compliance would have produced. Permission alone makes an agent that argues; the evidence requirement is what makes a refusal bankable instead of a conversation. It points at `code-review-subagent-fabricates-specifics-to-inflate-severity` for why evidence is still verified rather than trusted. **EXTENDED — `subagent-external-wait-orchestrator-takeover` (1.0.0 → 1.1.0), the most expensive lesson of the run.** That skill already covered a brief that *asks* an agent to poll. The commoner case is a **prohibition read as a command**: *"arm auto-merge and wait"* meant *do not fire `update-branch`* and was read as *poll until merged*. **Five agents did it in one night. One returned `meow~` three times having spent 300,000 tokens; another spent 355,000; a third reported individual CI legs going green as though that were progress. Their findings had been in the pull request body the whole time.** Worse than the tokens: **a polling agent emits notifications that look like progress**, so each one costs the orchestrator a wake to discover nothing happened, and an idle fleet looks busy. The fix is two instructions because one sentence cannot carry both — *"report immediately after arming"* and *"never fire `gh pr update-branch` while a run is in progress on that branch"* — and never write a prohibition whose verb ("wait", "hold off") could be read as an activity. **EXTENDED — `gh-issue-claim-coordination` (body only; description, tier and eval suite untouched).** The claim protocol records who *is* on something; the mirror image, telling agents what **not** to touch, had no protocol and fails in the opposite direction — instead of two sessions colliding, nobody works on it at all. **A pull request was on every agent's hands-off list for three days on the assumption that it belonged to another session. It was the user's own, opened the day before she left, and the instruction not to touch it is the entire reason nobody did.** An exclusion needs evidence of ownership read from the same primitives a claim uses (`gh pr view --json author,assignees`), never inferred from a branch name or a timing coincidence; "owner unknown" is the honest entry and is a question for the user, not a reason to freeze an item; and every hands-off entry needs an expiry and the same stale sweep a `wip` label gets. **Routing and hygiene:** all five new skills ship reference-only (`disable-model-invocation: true`), so the listing is unchanged at **7,741 chars against the 8,000 budget** and no tier is displaced. Each is named from the body of a live skill that owns its moment — `pre-dispatch-agent-isolation-parameter-not-prompt` gains three (the dispatch-window trio), `pr-conflict-from-mid-flight-merges` gains two (the `BEHIND`-not-`DIRTY` pair), and `gh-issue-claim-coordination` names the hold skill from its new section. Five skills added, none removed, none renamed. Total: 104 skills.

- **v1.29.0** (2026-08-12) — **51 skill names were cited here as if a reader could go and open them, and nothing had ever asked whether they existed.** No skill added, removed, renamed or re-scoped. **The measurement:** 51 backticked skill-shaped names across **77 sites in 41 files**, most in *"Sister skill:"* and *"See also:"* lines — precisely where a reader goes when the current skill did not answer their question, so each one costs that reader a search that cannot succeed. **Why no existing gate saw them.** `check_skill_routes.py`'s reachability check asks whether a reference-only skill is reachable **from** a live skill; this is the opposite direction, whether a name a skill points **at** exists at all. Its link check could not see them either: every one is a **bare backticked name**, not a `[...](../name/SKILL.md)` link, so a link-shaped gate fires on none of them and could never catch one in frontmatter. **The sweep is fence-aware, and the naive version is wrong in a way that looks right.** A plain `` `([^`]+)` `` regex pairs backticks **across** ``` fence boundaries — an odd number of backticks inside a fence flips the parity for every line after it. That version found the one independently-known dangling name in **2 of its 7** files, a 71% undercount that printed clean-looking output for the rest. The shipped sweep tracks fence state per line and pairs backticks only within a single non-fenced line; it was validated against that known 7-file ground truth **before** being trusted on anything else, and the naive form was re-run to confirm it still reproduces the 2-file undercount. **Both open questions the issue left are now answered.** *Were these created-then-deleted, or never built?* Two independent checks with a positive control that correctly recovers the one genuinely deleted skill: `git log --all` per name, and a sweep of every path in every ref. **None of them ever existed as a path in this repo's history** — so none is recoverable with `git show <sha>^:<path>`, and none can be retargeted against what a dropped skill actually said. *Which are external?* Every public repo under the author's account was enumerated and its tree read: **9 resolve** and are marked PUBLISHED with the repo that holds them — independently reproducing the issue's own `−9`. **NEW `.skill-citations-accepted`**, the baseline that lets this ship green, and the reader-facing map. It records **92** names in four groups with what each actually is: **9 published elsewhere** (verified by listing the repo's tree, not by recognising the name), **24 that are not skill citations at all** (a GCP IAM method, a labeler config key, a git trailer, a frontmatter field, a CSS property, deploy revision ids, branch and worktree names inside worked examples, two plugin *marketplace* names), **8 historical** (the v1.2.0 dropped-skill line and the v1.25.0 old→new rename mapping, which the changelog must keep or the history becomes wrong), **1 self-disclosed** (its own sentence already tells the reader no such skill exists), and **50 unpublished notes** — real notes the author holds, not in this repo's history and not in any public repo checked. **It fails closed on absence, unlike its two siblings.** A missing, empty or comments-only file is a **failure**, not a stricter policy: those entries are the deliberate citations the README must keep, so losing the file would turn every one of them into a false failure — the fastest way to get a check muted. `.hook-parity-accepted` is the opposite case and says so. **Twelve citations were fixed in the text rather than baselined**, because for these the reader can actually be sent somewhere. Six now carry the bundle prefix this repo already uses elsewhere (`superpowers:`, `roundtable:`) and five name the marketplace that ships them — **every prefix read out of that repo's own `marketplace.json` rather than invented**, which is how `roundtable` was confirmed as the plugin name inside `wan-huiyan/agent-review-panel`. The twelfth was a **misspelling**: one site read `gh-auto-deploy-…` where the other four in the same file read `gha-auto-deploy-…`, so the citation was wrong even as a name, and it sat in an instruction telling the reader to go and check it. **A defect in this release's own tooling, found and fixed mid-flight.** The generator that writes the accept file dropped any name it did not recognise into the "unpublished note" bucket **with that bucket's reason attached**. Two of the fixes above introduced backticked *marketplace* names, and both were written out as *"unpublished note; not in this repo's history nor any public repo"* — a confident wrong note of exactly the kind the file's own header warns against. A default bucket is a lie generator; it now refuses to write anything until every name is classified explicitly. **Verified by breaking it.** Thirteen mutations of the real files: a new dangling name in a SKILL.md, in the **hook's own README** (the scope hole an enumerated file list would have left), in **frontmatter**, in the `bundle:skill` form, and after an **odd-backtick fence** — the trap itself; plus the accept file deleted, emptied, comments-only and unreadable; the fence tracking disabled; and the markdown walk crippled to a single file. **Eleven must fail and all eleven do**, each attributably. Two must NOT fail and do not: a name inside a code fence (sample code, not a citation) and a real skill cited normally. **Not done, and named rather than left to be discovered:** the 50 unpublished notes are recorded but their 70-odd citing sentences still read as ordinary pointers, with no inline signal that the reader cannot follow them. The wording to copy already exists at one site — *"Not part of this plugin and not published; refer to it by name."* Closes **#39**. Total: 99 skills + 1 hook, unchanged.

- **v1.28.0** (2026-08-12) — **The repo computed the true skill count on every run and never compared it to the one it published.** No skill added, removed, renamed or re-scoped. **The drift.** The README's front page said **98 skills** against a tree of **99**, from v1.21.0 until v1.25.0 corrected it by hand — across the three most-read lines in the repo: the opening sentence, the install instruction, and the buckets sentence. The changelog disagreed with itself over the same window (v1.21.0 says 99, v1.22.0 says 98, v1.23.0 says 99), which is the tell that no check was ever involved. `check_skill_routes.py` already counted the skills on every run and already parsed the README's index rows; it knew the right number and simply never compared it to the published one. **What it now asserts:** every `N skills` claim **above `## Version history`** equals the number of skill directories. **The pattern has to tolerate an intervening link, and that is the whole reason this is not a two-line change.** The front page's first sentence reads *"A coordination toolkit of 99 [Claude Code](https://claude.com/claude-code) skills"*, and a bare `\d+\s+skills` regex does not match it. Measured on the real README: the naive form finds **2 of the 3** header claims, and the one it drops is **line 3** — the first sentence of the repo, the most-read claim in it, and the exact line this issue is about. A gate built on the obvious pattern would have reported OK through the entire window in which the front page was wrong, which is worse than no gate because the OK is what stops the next reader checking. **Scoped to the header, not the changelog.** Changelog entries state what was true at the time and must keep their historical figures; there are **45** matches below the heading, and a gate that fired on those is a gate that gets muted. If the `## Version history` heading cannot be found the check **fails** rather than scanning the whole file, because scanning it would turn every historical figure into a failure. **Three ways it refuses to pass quietly.** Zero claims found in the header is a **failure**, not a pass — either the front page stopped stating a count or the check stopped finding it, and both need a human. A missing `README.md` is a failure. And a **self-test runs first**, asserting the pattern still matches *both* the linked and the bare form: the zero-claims rule covers a header that stopped stating a count, but it does not cover a pattern edit that quietly stops matching the LINKED form while still matching the two bare ones — that would leave line 3 ungated while every message still read OK. A self-test whose fixture cannot tell the broken version apart is not a test. **No new CI step**, deliberately: this lives inside `check_skill_routes.py`, already CI's fourth step and already in the pre-push hook. A step of its own would have added another hand-maintained parity obligation to that hook, which is what v1.27.0 exists to police — the same reasoning that put `.leakdomains` inside `leak_scan.sh` in v1.26.0. **Verified by breaking it, because a gate that has never gone red is not a gate.** Nine mutations of the real files: changing each of the three header claims (including the linked one), renaming the `## Version history` heading, removing every header claim, regressing the pattern to the naive form, deleting `README.md`, and adding a 100th skill directory while leaving the README untouched — **eight must fail and all eight do**, each with the failure attributable to this check rather than a side effect of another. Changing a figure **inside a changelog entry** must NOT fail, and does not. Closes **#38**. Total: 99 skills + 1 hook, unchanged.

- **v1.27.0** (2026-08-12) — **The pre-push hook said it ran the checks CI runs. It was three behind, and the sentence saying otherwise was the only thing anyone had been checking.** No skill added, removed, renamed or re-scoped. **The drift.** `.githooks/pre-push` claimed *"these are the six checks CI runs, in CI's order"*. CI runs nine. The three it had never picked up — `check_release_parity.py`, the resume-gate pytest suite and the resume-gate mutation check — all arrived between v1.22.0 and v1.23.0, so the hook was wrong for three releases while its own header asserted *"a hook that runs fewer gates than CI is a hook that tells you a push is clean when it is not."* **This is the second time.** v1.19.1 found the same file running five of six and, correctly, fixed the hook rather than the sentence; that fix is still in place and the header still records it. Hand-maintained parity has now failed twice, which is the argument for a check rather than a third careful edit. **What the hook now runs.** All nine, in CI's order. `check_release_parity.py` runs unconditionally and needs no prerequisite check, because it already degrades on its own terms — *"could not read `gh release list` … NOTICE, not a failure"*, exit 0 — which is its pre-existing design, not a softening added here. The two pytest gates run under an interpreter that demonstrably has pytest, resolving `python3` if it already does and `uv run --python 3.11 --with pytest python` if it does not, and printing a loud four-line skip if neither is available. A hook that hard-fails on a machine without `GH_TOKEN` or `pytest` gets disabled within a day, and a disabled hook runs zero gates rather than six. **NO COUNT IS WRITTEN IN THE HOOK, IN `CONTRIBUTING.md`, OR HERE.** A hand-maintained total is right on the day it is typed and quietly wrong afterwards; "six" and the README's own "98 skills" against a tree of 99 are the same defect twice over. Both files now name the gates in a list and state no total, and `CONTRIBUTING.md`'s numbered list — itself a second copy of the same hand-maintained parity, missing the same three gates — was extended rather than recounted. **NEW: `check_hook_parity()`**, inside `.github/scripts/validate_plugins.py`. It parses the gates `ci.yml` invokes out of its `run:` blocks, parses the gates `.githooks/pre-push` invokes out of its code, and fails on anything CI runs that the hook does not, in the wrong order or not at all. **It is deliberately not a tenth CI step.** A step of its own would have been one more thing to keep in the hook — one more parity obligation of exactly the kind it exists to police. `validate_plugins.py` is CI's first step and the hook's first line, so it rides along with no new coupling; the same reasoning put `.leakdomains` inside `leak_scan.sh` in v1.26.0 rather than beside it. **Four mechanics, each there because the obvious version fails.** It reads **code, not comments** — both files document the gates they run, and a comment-blind scan reads each banner as invocations and reports perfect parity between two pieces of prose. It **fails closed on a dead input**: zero steps parsed from `ci.yml`, zero gate invocations from either file, or an unreadable input each fail rather than passing quietly, because a reformatted `run:` style or a renamed workflow would otherwise silently reduce it to nothing. It **compares order as well as presence**, since half the v1.19.1 defect was position — the leak gate ran last where CI runs it third — and only gates present in both are ordered, so an accepted omission cannot break it. And it **self-tests on every run** against a synthetic pair with one planted omission and one decoy in a comment: the zero-parse guards catch a parser that reads *nothing*, but not one that reads something and mis-keys it, and a normalisation that mapped every path to the empty string would have satisfied them and reported perfect parity forever. **Deliberate omissions are recorded, not muted.** **NEW `.hook-parity-accepted`**, one `key  # reason` per line, same format as `.release-parity-accepted`. It ships with a single entry: CI's `python3 -m pip install pytest` is environment provisioning rather than a gate, and the hook satisfies the same dependency by other means. An absent file accepts nothing, so unlike `.leakdomains` deleting this one makes the check **stricter**, and it needs no fail-closed guard of its own. **A separate fail-open found while wiring the mutation check in, and fixed here.** `mutation_check.py` scores a mutant KILLED on a non-zero pytest exit code and did not ask why the exit was non-zero. A missing pytest module also exits non-zero. Measured on an interpreter without pytest: it printed **"41 mutations, 41 killed, 0 needing attention"** and exited **0**, having executed no test at all — a perfect score from a harness that never ran, and the same shape as a leak gate printing `clean` because its denylist was empty. It now refuses unless pytest is importable **and** the unmutated suite passes, because if the clean tree already fails then every mutant's non-zero exit is inherited rather than caused. Adding it to the hook without that fix would have added a gate already known to lie on the maintainer's own machine. **Verified by breaking it, because a gate that has never gone red is not a gate.** Thirteen mutations of the real files: dropping each of four gates from the hook individually, moving the leak gate to the end (the v1.19.1 half-defect), adding a CI step the hook does not run, emptying `ci.yml`, emptying the hook, reducing the hook to its comment banner, stripping every `run:` block from `ci.yml`, and deleting `.hook-parity-accepted` — **eleven must fail and all eleven do**. Two must *not* fail and do not: a gate named only in the hook's comments, and `ci.yml` written with an inline `run:` instead of a block. The mutation-harness fix was checked in both directions too — refusal under a pytest-less interpreter, 41 of 41 killed under `uv`. Closes **#37**. Total: 99 skills + 1 hook, unchanged.

- **v1.26.3** (2026-08-12) — **The resume-gate blocked every push, merge and publish on a machine for a whole session, because the session had no transcript file to read.** No skill added, removed or re-scoped; the change is entirely inside the hook. **What happened:** a session started by another Claude Code process inherits `CLAUDE_CODE_CHILD_SESSION`, and an **interactive** session carrying that marker writes **no transcript at all** — the harness says so on screen and offers `CLAUDE_CODE_FORCE_SESSION_PERSISTENCE=1` as the remedy. `load()` then raised `FileNotFoundError`, which reached the fail-closed handler, and for `PreToolUse` **exit 2 blocks the tool call**. Every `git push`, `git -C … push`, `gh pr create|edit|merge`, `gh api`, `gh release create` and gated publish tool failed, in **every project on that machine**, with `gate failed (FileNotFoundError) - blocking to stay safe` naming the exception and nothing that would connect it to session persistence. It also fired on commands matching none of the ship conditions — three observed, all containing a `$` expansion, which the harness's `if` filter cannot evaluate statically. **Why allowing is correct rather than a softening.** This gate fires on a mark; a mark is only ever written by the `SessionStart` hook; that hook only acts when `source == "resume"`; and a session whose transcript was never written **cannot be resumed at all**. The `detect()` fallback has no rows to scan either. So the gate could not have fired in either direction — refusing the push does not make unreviewed subagent work reviewable, it only stops the machine. **But it does not go quiet**, because *"prefer blocking to a check that quietly did nothing"* is still right about its second half: a missing transcript now emits a `systemMessage` naming both the cause and the remedy, so a machine running with persistence off cannot end up silently unguarded. A bare `except FileNotFoundError: return 0, "", ""` would have reintroduced the exact fail-open defect this tool exists to avoid **while looking like the same fix**, which is why both halves carry their own mutation. **Only a missing file takes that path** — a payload with no `transcript_path` key (`KeyError`) and a path that is a directory (`IsADirectoryError`) still exit 2. **The two tests that pinned the old behaviour both used `/nope/missing.jsonl` as their stand-in for "any error"**, which is precisely why the two cases could not be changed independently; they now use a directory and an absent key, so fail-closed is still pinned by something that is genuinely an error. **Verified:** **94 → 98** tests green (measured on a pristine copy of the pre-change files, not counted by hand), and the mutation harness at **41 mutations, 41 killed** including three new ones — restore the block, keep the allow but drop the message, and strip the remedy out of the message — so each half fails on its own rather than being covered by the other. Confirmed live through the installed `hook-python` chooser: a missing transcript returns exit 0 with the message, a directory still returns exit 2, and the `$`-expansion command that had been blocked three times ran. `DESIGN.md` gains *The one failure that is not an internal error* beside the fail-closed table it qualifies, and the hook README a matching section — the old text said both hooks exit 2 on any internal error, and shipping this without them would have left the repo arguing against its own code. Total: 99 skills + 1 hook, unchanged.

- **v1.26.2** (2026-08-12) — **A disabled skill's description was 12 chars over the cap and CI said `over: False`, because the cap check scopes to model-invocable skills — and it is not the only one.** `subagent-pre-existing-misattribution` (v1.1.1 → **v1.1.2**) was **1,548 chars against a 1,536 cap**; trimmed to **1,482**, headroom −12 → **+54**, `--compare` reporting **5 triggers unchanged, 0 dropped, 0 narrowed**. Cut was explanation only: the two parenthetical glosses on the fix commands (`(did THIS pr touch it?)`, `(what was the value before?)` — which also carried a lowercase-`pr` typo) plus one reword, *"reading the current file state tells you nothing about"* → *"the current file state says nothing about"*. **Deliberately kept**, after an earlier draft of this trim cut it: *"and assumes your PR made a change it never made"* — the phrase closest to how a user would describe the symptom in their own words, and exactly the kind of narrowing a word-overlap metric cannot see, which is why the `--compare` rows get read rather than cleared with a score. **The finding this release actually carries is the count.** The cap check builds `live = [s for s in skills if not s.disabled]` on purpose — a disabled skill's description is not injected, so it consumes no listing budget — and the documented consequence is that it says nothing about a description that truncates mid-word the moment anyone enables it. Measured with the repo's own gate: **16 of this bundle's 78 disabled skills were over the cap**, not one, and the one named in the handoff was the **smallest overshoot in the set**. The largest is `git-rebase-stalls-async-post-commit-hook` at **2,153 chars — 618 characters that would die on enable**. This release fixes one of the sixteen; the remaining fifteen are enumerated with their exact char counts in [#42](https://github.com/wan-huiyan/agent-traffic-control/issues/42), **not trimmed here and not silently dropped**, because fifteen descriptions each need their own `--compare` read and a gate re-scoped to fail on all fifteen at once would ship red and be muted inside a week. `overnight-workflows` has one of its own (`frozen-cohort-rebucket-newer-model-contemporaneity-leak`, 1,637 chars).

- **v1.26.1** (2026-08-12) — **Twelve citations pointed at a skill dropped in v1.2.0, and two worked examples contradicted themselves.** No skill added, removed or re-scoped; no behaviour change. **(1) `pr-conflict-site-regen` was dropped in v1.2.0 as "too project-specific" and was still cited 13 times across 8 files.** All 13 were bare backticked names in prose or frontmatter — `git grep "](\.\./pr-conflict-site-regen"` returns **0** — so no link-shaped check could ever have caught them, and `check_skill_routes.py` does not validate backticked names at all. The 12 live citations are retargeted or rewritten; the 13th is the v1.2.0 changelog line recording the drop, which stays. **The finding that shaped the fix: retargeting everything to `merge-conflict-generated-files` would have been a lie at most sites.** The dropped skill is still recoverable — `git show 07472bf^:plugins/agent-squad-hr/skills/pr-conflict-site-regen/SKILL.md` — and reading it first was the difference between a correct fix and a plausible one. It covered four separable things, and v1.2.0's claim that *"the reusable kernel is already covered"* holds for exactly one: generated-output regeneration goes to `merge-conflict-generated-files` (**3 sites**, honest); ID collision during a rebase goes to `synthetic-id-collision-rebase` **with the gap stated in the text**, because that skill's recipe starts from `<<<<<<<` markers appearing while the dropped skill covered the *silent* form that produces no conflict at all and surfaces only under a post-hoc `grep | sort | uniq -c` audit; rebase cadence in a dense parallel-PR window plus the `gh pr merge --auto` footgun is covered by **nothing here**, so those citations are deleted, the sentences rewritten to stand alone, and the gap named where a reader will meet it; and an ID-naming convention that was itself dropped as project-specific is generalised in place. **Two citations pointed at things that never existed** — one cited "Step 2a/2c/2d/2e" when the recovered skill has no step past 2c, one cited "v1.8.0+" when its version never exceeded v1.4.0 and it lived for five days (added 2026-05-08, deleted 2026-05-13). Both dropped rather than guessed at. Every retarget destination was verified to exist as a directory, because in a fix for dangling references a dangling retarget is the obvious way to fail. **(2) Two worked examples contradicted themselves, and both were resolved against primary sources rather than by picking whichever reading tidied up.** In `stale-base-pr-silently-reverts-upstream-content`, one figure carried two nouns — a table cell and the grep pattern 100 lines below it disagreed. Both were born in the same commit, so the mismatch predates the v1.24.0 scrub, which mechanically renamed the grep's noun and never saw the table's. The real rendered source settles it, and so does the skill's own printed output: the grep as written matches under one spelling and returns nothing under the other. Fixed the noun; the figure is untouched. **The same file also mislabelled a local clock face as UTC** — an event dated `12:24:15Z` and called "64 minutes after" its predecessor was actually `11:24:15Z` and **4 minutes** after, and the document already disproved itself, since a later line dates the recovery at `11:34:25Z` and calls it "~10 min after". A 4-minute window makes the skill's point stronger, not weaker: there was almost no time to notice. In `handoff-prompt-stale-user-hint-newer-state`, a stated "3-event basis" named four events. The real correction reads *"2 of 3 still leakage-flavored; 1 of 3 clean"* — 1 + 2 = 3 — so the basis and the two flagged events were right, and the sentence claiming "2 of 3 are clean" was the error, having conflated that verdict with a separate proposal for a **replacement** basis one of whose members was never among the original three. Rewritten in already-scrubbed vocabulary, checked against the scrub commit's own mapping so no real identifier returns. **Listing budget unmoved at 7,741 / 8,000** — two edits are inside `description:` blocks and both of those skills carry `disable-model-invocation: true`, so the budget, which sums over live skills, does not move; stated because v1.24.0 got this number right for the wrong reason. **Deliberately not done:** one further dangling name is left in a sentence this release rewrote, because there is no honest retarget for it and deleting it would strip a real pointer — it belongs to the wider class, which is 52 dangling skill-name citations across 45 files, filed separately. Total: 99 skills + 1 hook, unchanged.

- **v1.26.0** (2026-08-12) — **The leak gate now carries a denylist CI can actually see, and it spans six industries so that it names none of them.** No skill added, removed, renamed or re-scoped. v1.24.0 removed 129 occurrences of engagement residue from 29 worked examples while `leak_scan.sh` printed `LEAK GATE: clean` before *and* after — correctly, because it enforces three generic patterns plus a `.leakterms` file that is **gitignored**, so a CI checkout degrades to the three and answers a narrower question than the one being asked of it. Nothing stopped the same vocabulary returning in the next worked example. **NEW `.leakdomains`**, read by a fourth scan inside `leak_scan.sh`: a **tracked** pattern file, one `grep -E` regex per line, matched case-insensitively and word-bounded. **The point of the design is the two properties `.leakterms` lacks.** It is *committed*, so CI and a fresh clone both read it — a term list nobody can see is not a gate. And it **fails closed**: a missing `.leakdomains` exits 1 with a message telling you to restore it rather than delete the check that noticed, because *"the denylist wasn't there"* reading as *"clean"* is the exact failure being fixed and it must not happen twice. It also lives **inside** `leak_scan.sh` rather than as a tenth CI step, so it needs no matching edit in `.githooks/pre-push` and cannot drift out of a parity that is maintained by hand. **Why it spans education, health, finance, legal, HR, retail and cross-sector privacy statutes, and why trimming it to "the sectors we work in" would undo v1.24.0.** A denylist naming one sector **is** the disclosure. Committing that sector's nouns and nothing else to a public repo reassembles, in one searchable tracked file, the set the scrub had just removed — and points at the engagement more precisely than the prose ever did, on the same repo, in a file whose whole purpose is to be read. This changelog's own back-catalogue is what taught that lesson: v1.24.0 had to fix an entry that printed a pre-scrub symbol beside its post-scrub replacement and handed over the mapping for free. A list spanning six sectors points at none of them, and it has a second job — the next engagement will be in some other industry, and the guard is already waiting for it. The rule the file states, and the only one it enforces: **a worked example here is about a coordination failure, not about the client it was learned at; it may name a git object, a CI system, a file format or a tool, and it may not name an industry.** **It ships green**, which is the only reason it can ship at all: every pattern is zero-hit on the tree today, so the gate starts clean and cannot be muted as noise inside a week — before the scrub it could only have started red. **Six candidate terms were rejected and are recorded in the file with their reasons**, because each collides with this repo's own engineering vocabulary and could not stay zero-hit: the word for a session that *claimed* an issue, the word for a Claude Code session log (**134** hits), the word for working out why something failed (19), the word `check_skill_tiers.py` uses for its own per-class caps (5), the word for git object accumulation (3), and one dropped as simply too common in ordinary pricing prose. A term that cannot stay zero-hit belongs in `.leakterms`, not here. **Verified by injection, because a gate that has never gone red is not a gate**: one term from each of the seven groups planted in a real skill turns the gate red and names the pattern that caught it, and the file does not flag its own contents. **Review then found that the first version of this failed its own headline invariant, which is worth recording rather than quietly fixing.** *"Fails closed"* had been implemented as *"errors if the file is absent"* — and absence is only the most obvious way for a denylist not to run. Five other ways each printed `LEAK GATE: clean` with planted vocabulary sitting in the tree: an **empty** file, a **comments-only** file, an **unreadable** file, a **malformed regex** (grep exits 2, the redirect swallows the message, and no output reads as no hits), and a **stripped final newline**, which silently drops the last pattern because `read` returns false at EOF. Every one is the *same* failure this release exists to fix, reproduced inside the fix. The loop now counts the patterns it actually loaded and refuses a run that loaded none, checks grep's exit code per pattern and fails on an unusable one, tolerates a CR line ending, and reads a final unterminated line. All six modes are re-verified red. **`CONTRIBUTING.md` is no longer exempt from the scan either** — it had been excluded since the gate was written, back when it only *described* the gate; this release moved the sector policy into it, which made the one file most worth scanning the one file that was not. Note while reading `EXC`: `--exclude` matches on **basename**, so each entry exempts that filename in every directory, not only at the repo root. **What this does not do, stated here rather than discovered later.** It is a known-term grep and therefore the **necessary half only**. It cannot catch an **open-vocabulary coined name** — a dashboard nav label, a git worktree name, a campaign token in a filename, a module or dataset path — and *that* class produced most of what v1.24.0 removed; no fixed pattern can enumerate a word it has never seen. It also says nothing about whatever sector you are working in right now unless that sector is already listed. `CONTRIBUTING.md` gains a table contrasting the two term files, and the instruction that a hit on industry vocabulary is fixed by rewriting the example, not by deleting the term. Total: 99 skills + 1 hook, unchanged.

- **v1.25.0** (2026-08-12) — **Five skills shipped with names longer than the format allows, and nothing here checked from v1.7.0 to v1.24.0.** The [Agent Skills specification](https://agentskills.io/specification) caps a `name` at **64 characters** and requires it to *"only contain unicode lowercase alphanumeric characters (`a-z`, `0-9`) and hyphens (`-`)"*, to *"not start or end with a hyphen"*, to *"not contain consecutive hyphens"*, and to *"match the parent directory name"*. This repo shipped five at **72, 71, 71, 70 and 65**, all of them landing on **2026-07-08 in v1.7.0** except one that arrived 2026-08-05. Claude Code's own skill docs state **no** cap, which is both why nothing local noticed and why this is a **portability** gate rather than a house-style rule: an over-long name loads fine in Claude Code and fails a spec validator anywhere else. The existing checks could not have caught it either, and each passed truthfully — every one of the five matched its own directory name, resolved from every link pointing at it, and carried exactly one README index row, so `validate_plugins.py` had nothing to say. Nor did the other two, and for a plainer reason for most of the window: **`check_skill_routes.py` did not exist until v1.16.0 and the tier gate until v1.19.0**, both on 2026-08-07, a month after the first four of these names shipped. **Renamed, bodies unchanged apart from one cross-reference line:** `workflow-pipeline-parallel-stage-returns-bare-array-dropped-by-collector` → `flatten-workflow-parallel-arrays-before-collecting`; `workflow-large-markdown-deliverable-extract-from-output-json-not-retype` → `extract-workflow-markdown-from-output-json`; `verifying-subagent-in-your-live-worktree-measures-your-uncommitted-work` → `isolate-subagent-verification-from-live-worktree`; `parallel-session-superseded-validated-fix-verify-live-prod-state-first` → `verify-live-prod-before-shipping-superseded-fix`; `pytest-editable-install-resolves-to-primary-checkout-not-worktree` → `verify-pytest-imports-worktree-not-primary-checkout`. **This paragraph is the only place that mapping exists** — the v1.15.0, v1.9.0 and v1.8.0 entries below keep the old names deliberately, because a changelog records what shipped at the time; that is the same treatment v1.2.0's dropped-skill line already gets. **`validate_plugins.py` check 6 now enforces the whole name rule** — length, character set, and no leading, trailing or doubled hyphen — where before it enforced only *name equals its directory*, which is the half that was already true. **The listing budget does not move: 7,741 of 8,000**, because all five carry `disable-model-invocation: true` and the budget sums over live skills — the same reasoning v1.24.0 had to correct about two edited `description:` blocks. **30 references were retargeted across 13 files**, re-derived against current `main` rather than copied from the branch this work was drafted on: that draft listed 10 files, and current `main` carries references in **five it never opened** (`git-worktree`, `pr-plan-bucket-triage-before-sizing`, `recover-killed-session-from-transcript-and-worktree` and two eval suites), because 17 commits landed in between. **Also corrected here, because a release whose thesis is "nothing here checked" is the wrong one to ship a wrong front-page count in: this README's own opening lines said 98 skills.** The tree has held **99** since v1.21.0, and this changelog has been saying so inconsistently ever since — v1.21.0 says 99, v1.22.0 says 98, v1.23.0 says 99. Lines 3, 21 and 31 now say 99, which is what `check_skill_routes.py` counts. Nothing gates that number, which is why it drifted; that gap is filed rather than fixed here. **How this was done matters more than the rename.** The work existed as **staged-but-uncommitted** renames in a local worktree pinned **17 commits behind `main`**, on a base predating the v1.24.0 scrub. Committing from that tree would have carried **pre-scrub** file content under a **new** filename, which is the shape hardest to catch: the diff reads as an innocent rename, a `git diff --diff-filter=D` gate misses it because a rename reports as `R100`, and the new path has no history on `main` for a reviewer to compare against. So the renames were **redone** as fresh `git mv`s on current `main` — a rename of current-tree files cannot carry stale content — and the stale tree was retired rather than rebased. Verified before push: `git diff --diff-filter=DR --name-only origin/main...HEAD` lists five renames and no deletion, and the five old names survive nowhere in the tree except the three changelog entries named above. Total: 99 skills + 1 hook, unchanged.

- **v1.24.0** (2026-08-11) — **The worked examples carried the engagements they were learned in, the leak gate said clean the whole time, and the first pass at fixing it missed the README.** No skill added, removed or re-scoped; no behaviour change. What moved is the *content* of 29 skills and one eval suite — **111 insertions against 111 deletions**, because every edit is a substitution and nothing was deleted to make the problem go away — plus four occurrences in this changelog's own back-catalogue. **129 occurrences in total.** None of it was a credential, a key or a customer's name. It was **shape**: a serving-dataset path, a CRM feature-table name, two module filenames, a Dataform `.sqlx` name, a dashboard nav label and its chart id, a derived status field, a git worktree name, a real session UUID, a coined deliverable word, and a sector-specific vocabulary (a privacy statute among it) recurring across eight examples. Any one is innocuous; the set names an industry, a data stack and a problem, which is enough to identify an engagement. A second, unrelated private project was named **outright** in seven files, and a client codename in one more — the codename sitting three words from a repo name someone had already neutralised, which is what residue looks like when a scrub is done by hand and stops early. **The gate passed on every one of these, and was right to.** `leak_scan.sh` enforces three generic patterns — Salesforce `__c`/`__r`, API-key shapes, email addresses — plus a `.leakterms` file that is **gitignored**, so in a CI checkout it degrades to those three. "LEAK GATE: clean" is a true statement about a narrow question. Nothing here matched any of the three; the gate ran green before this change and after it, which is the point of recording it. That is this repo's own *"a known-term grep is necessary, not sufficient"* rule landing on the repo that states it. **Why the count moved three times.** A hand-grep found 4 skills; a wider one found 9; **twelve independent semantic readers over all 146 text files found 130 candidates in 29**, and a verification pass opened every one — dropping **10** (2 at line numbers where the token does not exist, 8 generic infra the rule protects), correcting **15** line numbers, and catching **7 sites all twelve readers missed**, including an entire second copy of one worked example and half a coined tier taxonomy whose other half was already being replaced. **Then a four-lens review found two more, and both are the interesting ones.** (1) **The sweep that declared the tree clean had excluded `README.md`** — and this changelog's own v1.15.0 entry named the second project outright, printed its coined symbols, and carried the domain word, in the file more people read than any skill. It also still showed the **pre-scrub** symbol beside the skill's **post-scrub** one, which hands over the old→new mapping for free and defeats the rename it was documenting. A change-scoped sweep cannot see a leak that predates the change; that is the whole reason the rule says to verify the *whole* artifact, and the sweep had been narrowed to the directory being edited. (2) **A rename broke the grep that hunts the renamed symbol** — `grep -i <old-stem>` left sitting above a printed output line containing only the new one, inside the skill about greps that miss, in a section titled "where the needle comes from". The near-miss the lesson turns on — a recalled symbol sharing a stem with the real one — had also gone, so the sibling symbol was renamed too and the stem relation restored. Both were caught by review, not by the gates, and neither is a class a counting assertion can catch: **an occurrence count protects the tokens you listed, not the ones you failed to list.** **Every measurement is kept** — counts, timings, percentages, multipliers, dates, PR and issue numbers, commit SHAs — verified by diffing every number on a removed line against every number on an added one; the only numeric loss in the tree is the hex of the replaced session UUID. Replacements were applied by a script asserting an expected occurrence count per rule, which **aborted before writing on its first run**: 11 counts were wrong, two of them strings that wrap a line break and had therefore never matched anything. Consistency is enforced across files rather than within them — a label appearing in two skills gets one replacement — and where a lesson depends on a shared word stem (a column that must be *absent* while a widened grep *hits*), the stem moves intact or the lesson dies. **`listing_chars` is unchanged at 7,741 of 8,000**, but not for the reason first written here: **two `description:` blocks were edited**, and the budget is unaffected only because both skills carry `disable-model-invocation: true` and the budget sums over live skills. The number was right and the reason was wrong — which is precisely why both forms of the description gate are run, the text report's exit code covering only model-invocable skills. **What this does not do:** these files have been public for weeks. A scrub stops *further* exposure and nothing else. The pre-scrub blobs stay reachable by direct SHA, in any fork, and in GitHub's cache; removing them would mean rewriting public history and re-cutting tags and releases, which is a separate deliberate act and is not this. Total: 99 skills + 1 hook, unchanged.

- **v1.23.1** (2026-08-11) — **A timestamp in v1.23.0 was the wrong field, on four surfaces.** Fact-check caught it. The v1.23.0 narrative said the drift guard went red on `43f4d62` at `2026-08-07T10:56:43Z`. That is the workflow **run's `created_at`** — when it started. The check-run's **`completed_at`**, the moment it concluded `failure`, is **`2026-08-07T10:56:58Z`**. Corrected in all four places it reached: `release.yml`'s header comment, `scripts/check_release_parity.py`'s docstring, `.release-parity-accepted`'s note on 1.13.0, and the v1.23.0 entry above — and the field is now named explicitly at each site so it cannot drift back. Nothing about the v1.13.0 diagnosis changes: the guard did fire on that commit, the release was still lost, and the fix still stepped over the value. **What changes is the class of error**, which is the same one v1.23.0 exists to document at a smaller scale: a number genuinely true of *something* — the run's start — but not of the thing the sentence described. Reading the wrong field is the cheapest way to publish a real number that means nothing.

- **v1.23.0** (2026-08-11) — **The release automation lost a release for three months, and it lost it by not running.** No skill changes. `release.yml` filtered `paths: ['VERSION']`. `43f4d62` shipped **v1.13.0** in `marketplace.json`, `plugin.json` and the README while leaving `VERSION` at 1.12.0, so the workflow **never triggered** — no failed run, no skip message, nothing in the Actions list to notice. `6a49203` then moved `VERSION` 1.12.0 → 1.14.0, stepping **over** the value, so `1.13.0` never existed on `main` and no later push could cut it either. **The drift guard did fire**: `validate_plugins.py`'s `VERSION != plugin version` check went red on `43f4d62` itself (check-run `completed_at` 2026-08-07T10:56:58Z, conclusion `failure`), a human saw it, and the fix bumped to 1.14.0 — satisfying the guard while leaving the release permanently unreachable. *A guard tells you a state is wrong; it does not tell you what that wrong state already cost.* Two changes. **(1) The release job now runs on every push to `main`.** The path filter was the worst available failure mode here, because the one commit shape that most needs a release — manifests bumped, `VERSION` forgotten — is exactly the shape that suppressed it. Running unconditionally is safe: the create step is idempotent, and the cost is a ~15s no-op per push. Note recorded in the workflow itself: **both branches of that step exit 0**, so the job's `conclusion: success` is byte-identical whether it cut a release or skipped one — the status field answers a different question than the one being asked of it. **(2) NEW `scripts/check_release_parity.py`**, wired into CI — the first check here that looks *backwards* at damage already done rather than forwards at the next commit. It asserts every version this repo has claimed to ship has a matching release, over the **union** of the `VERSION` history and the README changelog. The union is load-bearing and a first draft got it wrong: **neither source alone detects v1.13.0.** Measured — VERSION values with no release: **3** (1.3.0, 1.4.0, 1.5.0); README versions `VERSION` never held: **4** (1.0.0, 1.1.0, 1.2.0, 1.13.0); union with no release: **7**. A VERSION-only gate reports the three and misses the case it was built for, which would have been a guard that cannot fail on the case that prompted it. **Baselined in `.release-parity-accepted`** so it ships green and fails only on new divergence — a gate that cannot go green gets muted inside a week, and a muted gate looks like coverage. The baseline records what was verified rather than what was plausible: 1.0.0–1.2.0 genuinely pre-date both `VERSION` and the automation (`435ffaa` introduced release.yml **and** set VERSION=1.3.0 in one commit); 1.3.0/1.4.0/1.5.0 are marked **cause unestablished**, because that same fact makes "pre-automation" false for them and a confident wrong note is what someone trusts later instead of re-checking; and 1.13.0 is marked **unfillable by the automation** with the manual `gh release create --target` command to close it honestly. The gate was verified by making it fail on purpose — an injected changelog entry for an unreleased version exits 1 and names whether the claim came from `VERSION` or the changelog — then confirmed green again. Total: 99 skills, unchanged.

- **v1.22.0** (2026-08-10) — **First hook in the bundle: `resume-gate`.** Every skill here fires because someone reaches for it; this covers the case where nobody has anything to reach for. A subagent killed mid-run leaves partial output in the parent's transcript, and a resuming session reads it as a finished result — `recover-killed-session-from-transcript-and-worktree` has always documented the recovery, and had no trigger. The hook is that trigger, and the two skills are cross-linked both ways. **Three designs; the first two were built far enough to be measured, then rejected, and DESIGN.md keeps them because the rejected ones are the intuitive ones.** Draft 1 (file mtimes + a marker file cleared by an evidence file) died on eleven defects, four worth repeating: `find -newermt` **fails silently in both directions on darwin** — the ISO form with `Z` that transcripts emit gives `Can't parse date/time`, rc=1, zero output, so a script ignoring the return code reads "nothing unreviewed" and never arms, while dropping the `Z` slips the cutoff ~6 hours early with no error at all; its core invariant is **empirically false** (`AGENTS.md` in all seven worktrees on the control machine carried one identical mtime); **294 files postdate the cutoff in one worktree, 6,043 from the repo root**, so one evidence line per item is not a workflow; and the evidence file is **cleared by the party being gated** — a 31-character sentence opens it, which is not a gate but a prompt to type a sentence. Draft 2 (a bare scan for the harness's partial-output notice) died on one false positive and one false negative, both fatal: of **1,898 transcripts four contained the string, two genuine incidents and two the design conversation itself**, and draft 2 printed that string plainly in its own design document — so every session that read the document would have poisoned its transcript permanently and gated itself with **no clearing path**, because the design had deliberately removed all state; and it **misses async subagents entirely**, since **81 of 181 Agent calls return only `Async agent launched successfully`** and their outcome arrives as a task-notification instead. The motivating incident killed two subagents, one of each kind; draft 2 caught one. **What shipped:** two signatures behind one detector — prefix-**anchored** partial-output notices (true positives carry the string at offset ~124 of a ~500-char result, discussion at ~2,005 of ~14,700; measured on the design session's own transcript, 15 needle rows, 2 inside an Agent `tool_result`, **0** surviving the anchor), plus a task-notification whose status is not `completed` AND whose `tool-use-id` resolves to an Agent call. Both extra conditions are load-bearing: a **bare enqueue/`remove` imbalance fires on 57 of 79 sessions (72%), including 3 hits on a clean control**, because a background agent still running at session end leaves one behind — that is normal, not a hazard (status tally across all 1,898 transcripts: `completed` 225, `killed` 24, `failed` 9, `stopped` 1); and a backgrounded **Bash** call produces a task-notification too, which surfaced as an unactionable `(async task toolu_…)` and armed the gate over a shell command the session had already seen inline, so the fallback label is deleted outright rather than improved. Gate strength is `ask`, not `deny` and not on commits: `deny` is advisory and any self-clearing path is walkable by the gated party, which is precisely how draft 1 failed. **Also corrected in the record:** the incident that motivated all of this was **misdescribed by its own author** — a local-time file mtime was compared against a UTC transcript timestamp, so a write that looked like 39 minutes *after* the parent went quiet was 21 minutes *before* it, and the subagent never outlived its parent at all. The residual hazard is narrower and real, and it is what the tool detects. **The fixtures are generated, not captured** — the first version froze whole real transcripts (**21.6 MB**, permanent in git history), the second sliced them, and the published version builds them deterministically with no clock, randomness or filesystem reads; a test regenerates and compares byte for byte, so a hand-edited fixture is a failure rather than a silent divergence. Two fixture properties are pinned by their own assertions because otherwise the tests depending on them pass **vacuously**. **94 tests and a committed mutation harness — `tests/mutation_check.py`, 38 mutations, all killed — both now enforced in CI**, which previously ran no tests at all for this repo. The harness is committed precisely because the first version of this entry was not trustworthy: it claimed "25 mutations, 25 killed" with no list published, and an **independent review then ran a different set and 7 of 13 survived**, each a real gap — including one where nothing distinguished `count > 0` from `count >= 0` (every balanced pair in the fixture was `completed`, so the status gate masked the balance gate), and one where the test *named* for a corrupt payload never reached the decode branch it was named after, because the non-base64 noise made the capture group match empty and `b64decode("")` succeed. A quoted score nobody can re-run is not a check. **That review also found two fail-open defects in code that had already passed its author's own 25 mutations.** (1) An **undecodable segment mark silently disarmed the gate**: `read_last_mark` correctly returned "no trustworthy state", but the fallback `detect()` still windowed on the most recent mark — the corrupt one — leaving it the handful of rows after it to scan, usually none. Appending one bad mark to the incident fixture took detection from two hits to zero and the decision from `ask` to silence, and two realistic triggers reproduce it: a truncated mark, and a description containing the mark's own separator, i.e. the hook corrupting state it wrote itself. The general lesson outlives the bug — *"returns None so the caller re-derives" is only safe if you check what the caller re-derives over*; both halves were individually reasonable and the composition was fail-open. (2) `install.py` **checked that the install path was stable but never that the script existed**, so copying only `install.py` (its own printed remedy is a two-file `cp`) wrote a config in which every push, merge and publish on the machine exits 2 — the exact disaster its docstring warns about, reached *through* the guard rather than around it. Also fixed: the install path check now resolves symlinks (it refused the correct directory on systems where `$HOME` sits behind one, while telling the user to copy files into the directory they were already in), `main()` writes a backup before touching the config rather than mentioning one only in the message where nothing is written, and non-dict `message`/`input` rows can no longer raise through `PreToolUse` and block every gated command. One more, cosmetic but load-bearing to a stated rule: **`"first half" + "second half"` is folded by CPython at compile time**, so the "assembled at runtime" needle was sitting whole in the `.pyc` of any checkout that had run the tests — gitignored, never shipped, and the docstring was still false; it now uses `str.join`, which the peephole optimiser leaves alone. The segment mark gained the anti-leak rule the needle already had, since any committed file quoting an *empty* mark would disarm the gate of every session that read it. The hook is **opt-in**: not registered plugin-natively, so installing or updating this plugin changes nothing until you run the installer, and the installer **refuses to run from a plugin cache, a worktree or `/tmp`** — the hook config stores an absolute path, a `PreToolUse` hook that exits 2 blocks the tool call, and the first install of this tool pointed at a scratch worktree that would have blocked every push on the machine once removed. **Known unverified:** the `Bash(git -C * push:*)` mid-pattern glob is not confirmed against a primary source; if the syntax is invalid that one handler silently never fires and the other seven are unaffected. No skills added or removed; `recover-killed-session-from-transcript-and-worktree` goes 1.0.0 → 1.1.0 for the cross-link. Total: 98 skills + 1 hook.
- **v1.21.0** (2026-08-10) — **One new skill, and its stated mechanism corrected before it shipped.** NEW: `stale-base-drops-rows-from-a-shared-ledger` (1.0.0) — a branch drops other sessions' rows from a shared hand-edited ledger (a tracker `data.js`, a registry, a CHANGELOG) with **no file deletion, no conflict and an ordinary diffstat**, so a file-level `--diff-filter=D` gate reports clean. That gap is real and is the reason to have the skill: **the deletion gate catches removed *files*, it says nothing about reverted *content*.** **But its original Problem section said the loss happens because "your branch's version does not contain them — on squash-merge, your version wins wholesale", and that is false.** Measured on a synthetic two-session ledger: a branch that appends its own row from a stale base **conflicts** (`merge-tree` exit 1); a branch that regenerates the file from a stale copy **also conflicts** (exit 1). Only a stale **TREE** under a current parent (`git reset --soft`, or committing an old worktree with `git add -A`) merges cleanly — exit 0 — and loses the rows, and it does so because the branch's commit *genuinely records* removing them: the merge-base diff literally contains `-{id:'D'}` and `-{id:'E'}`. Git is not preferring your side; it is doing what your commit says. The skill now leads with that distinction, carries the three measured cases as a table, and cross-references `git-diff-2dot-vs-3dot-merge-safety`, which gains the reciprocal note — **`--diff-filter=D` empty does not mean nothing was lost**, plus `--diff-filter=DR` rather than `D`, because a rename reports `R100` and slips past a `D`-only filter. Also corrected: the **v1.20.0 entry's budget caveat**, which said the listing was over `skillListingBudgetFraction` at 23,174 chars — that figure predated the v1.18.0 listing rewrite; the listing now fits at **7,741 against 8,000**, and the promoted skill carries `listing_tier: name-led` to keep it there. Total: 99 skills, one added.

- **v1.20.0** (2026-08-10) — **A skill taught the wrong mechanism, and the skill that taught the right one was the only one switched off.** No new skills; eight corrected. **(1) `git-diff-2dot-vs-3dot-merge-safety` (1.0.0 → 1.1.0) is now MODEL-INVOCABLE.** It carried `disable-model-invocation: true`, so the correct statement of the rule never reached the model's listing, while sibling skills that prescribed the WRONG command loaded normally. That is backwards for a rule whose whole job is to fire *before* someone acts on a misleading diff. Its own six 2-dot examples stay exactly as they were — each is annotated "misleading"/"WRONG" and is the point of the skill. Caveat, corrected in v1.21.0: that entry originally said the listing was over `skillListingBudgetFraction` at 23,174 chars. It was measured before the v1.18.0 listing rewrite landed. **The listing now fits — 7,741 chars against the 8,000 budget, 259 to spare** — and this skill carries `listing_tier: name-led` to stay inside it. Being under budget makes the skill *eligible*; admission is still ranked by usage across every installed plugin, so it is not a guarantee that its description is shown rather than collapsed to a bare name. **(2) `pr-from-stale-branch-silently-reverts-newer-main-files` (1.2.0 → 1.3.0) — its headline claim was FALSE and is retracted.** The Problem section said *"git computes the diff as `origin/main..HEAD` — and because your branch lacks B/C/D, the diff shows them as DELETIONS. Merging the PR removes B/C/D from main."* A three-way merge preserves B/C/D, and GitHub computes the merge-base (3-dot) diff, not 2-dot. Falsified four ways: for one PR GitHub reported **5 files / 219 insertions / 16 deletions** and `git diff --stat <base>...<head>` matched **exactly**, while `git diff --stat origin/main..<head>` reported **4 files / 13 insertions / 22 deletions** — a different file list and 22 deletions that did not exist; 3-dot is not the lax option either, since a branch genuinely deleting another session's 418-line file still shows **418 deletions** under it; and a controlled repo reproduced the real trap end to end (ancestor gate exit **0**, `rev-list --count` **0**, three files silently deleted). What is real is the **stale TREE under a current POINTER** (`git reset --soft origin/main`, or committing an old worktree with `git add -A`) — the commit *records* the deletion, so the merge applies it, and every behindness check passes. That is now the headline; "your branch lacks B/C/D" is named explicitly as what does NOT cause it. **Example 1 (the S258 case) is retracted**: its 5 "deleted" files (−384/−93/−65/−40/−19) were a 2-dot artifact, nothing was ever at risk, and the clause *"a clean merge would have reverted all of S257b's work"* is gone. **Example 2 (PR #853, 59 files / 5,081 deletions) stands** and now says why — its parent WAS main's tip, so 2-dot and 3-dot were identical there and the deletions were genuine. The *"merge origin/main before every PR"* advice is amended rather than deleted: worth doing to surface conflicts early and to test the tree that will actually land, but it is a **no-op** against the stale-tree case and against the merely-behind case there was nothing to prevent — it fixes the measurement, not the risk. **(3) Six mechanical `..` → `...` corrections**, each read in context first: `subagent-pre-existing-misattribution` (1.1.0 → 1.1.1) at three sites, where the **body contradicted its own frontmatter**, which has prescribed the 3-dot form since 1.1.0 — and a 2-dot diff there manufactures exactly the misattribution the skill exists to stop; `docs-branch-off-feature-branch-smuggles-code` (1.1.1 → 1.1.2) ×2; `synthetic-id-collision-rebase` (1.1.1 → 1.1.2); `code-reviewer-subagent-no-bash-blocked-on-pr-diff` (1.0.0 → 1.0.1), where the pre-generated diff file IS the reviewer's ground truth because it has no Bash to cross-check, so it must match GitHub's Files-changed tab; and `gha-pr-merge-ref-shows-upstream-changes` (1.0.0 → 1.0.1), whose description asked "is main ahead of me by N commits?" via `git diff`, which cannot answer that at all — replaced with `git rev-list --left-right --count origin/main...HEAD`, the command **the same file already prescribed at line 47**. **Two dots were left alone everywhere they are correct**: `git log A..B`, `git rev-list --count HEAD..origin/main`, an explicitly-computed merge-base followed by 2-dot, and two fixed SHAs both on main. **(4) `worktree-stale-local-main-ref-inflates-pr-diff` (1.1.0 → 1.2.0) — an overclaim corrected, and the pairing made explicit.** Its Verification said `merge-base --is-ancestor <base> origin/main` returning 0 proves "the squash/merge won't revert upstream". It does not: the stale-tree variant satisfies that condition exactly and still deletes files. The check that DOES prove it — `git diff --diff-filter=D --name-only origin/main...HEAD` returning empty — is now the first Verification line. This skill is also the necessary caveat on the whole 2-dot rule: 3-dot **over**-reports when the LOCAL `main` ref is stale (its measured case: **92 files / 8,381 insertions** for a 6-file commit), so switching operator without fetching trades one false alarm for another. All four skills now cross-reference each other. Total: 98 skills, none added, none removed, none renamed.
- **v1.19.1** (2026-08-07) — **Housekeeping on three claims about the tooling: one wrong, one unverifiable, and one the hook itself contradicted.** `CONTRIBUTING.md` said the vendored `check_skill_descriptions.py` was "currently v2.2.1"; the file's own header says **upstream commit `c413fd4`, upstream version 2.3.0, re-vendored 2026-08-05**. That is two re-vendorings stale, and not cosmetically: v2.3.0 changed *what the gate scores* — wrap corruption is now measured over every skill, disabled included — and added a NO HEADROOM tier. The page now tells you to read the version out of the file rather than out of the page. Second, and recorded as a **gap rather than a finding**: that header pins `sha256 f72dcfa…` and says a parity test can strip the note between its `--8<--` markers and hash the rest, but **no such parity test exists in this repo and the stripping convention is not written down** — whether the docstring's opening line, the marker lines, or the surrounding blanks are included changes the hash. Stripping the marker block and hashing the remainder gives `5a592be…`, which is evidence about a guessed convention and **not** evidence of a stale fork; the note says so of itself, and warns that a feature-presence grep is no substitute because a test asserting `find_wrap_corruption` and `compare_descriptions` were present once stayed green on a copy that genuinely was one. What is owed is the test. Also added a **release section**: `VERSION`, `marketplace.json` and `plugin.json` must move in one commit — that drift reddened `main` at v1.5.0 and again on the v1.8.1 branch — plus the two ways it went wrong here, bumping two of three, and `open(p,'w').write(open(p).read()…)`, which truncates before it reads and left both manifests at zero bytes during v1.19.0 until `validate_plugins.py` caught it. **Third, `.githooks/pre-push` said it ran "the same checks CI runs, in the same order" and did not** — five of the six, missing the `within_budget` assertion entirely, with the leak gate last where CI runs it third. The comment was the thing arguing that this matters ("a hook that runs fewer gates than CI is a hook that tells you a push is clean when it is not"), so the hook was **fixed rather than reworded**: all six now run in CI's order, confirmed by running it end to end. Nothing enforces that parity, and the comment now says so instead of implying otherwise. Docs plus the hook; no skill or gate script changed. Total: 98 skills.
- **v1.19.0** (2026-08-07) — **v1.18.0 got the listing under budget; this stops it drifting back out, which it would do silently.** Every live skill now declares `listing_tier: rich | short | name-led`, and a new gate `scripts/check_skill_tiers.py` (CI step 5) fails on seven conditions: a live skill with no class, or a class declared on a reference-only skill; a headcount over **8 rich / 8 short / 10 name-led / 24 live** — the message names the current holders, because **promoting a skill means naming the one it displaces in the same pull request**; a description over its class ceiling; a listing total over target, recomputed with the vendored gate's own formula and taking `--context` and `--bytes-per-token`; a reference-only skill with no route in from a live body; and cross-link or provenance boilerplate (`Covers:`, `See also`, `Core insight`, `Demonstrated ROI` …) inside a description, which is resident in context every turn and buys nothing. Reachability is **delegated** to `check_skill_routes.py` rather than re-derived — two gates that each decide locally what "reachable" means promptly disagree. **The target is 7,780 against today's 7,542, and the gate now says what those 238 chars buy: one more `name-led` skill and nothing else.** An entry costs `len(name) + 4 + len(description) + 1`, so a name-led entry at its ceiling costs `len(name) + 165` — 177 to 237 across this repo's name lengths, and 238 covers the longest of them; a `short` one costs up to 357 and a `rich` one up to 677, so anything but a single name-led addition means shortening something in the same PR. (The first version of this gate targeted 7,643, which left 99 chars: the next genuine addition would have failed CI while the docstring beside it promised room to grow.) The 220 chars between the target and the 8,000 hard ceiling are a warning band, not capacity. `--why` prints the whole slate with each skill's size, its inbound routes from live skills, and its usage count; all 7 rich skills carry a usage record. It does **not** follow that a usage record means `rich`: `using-git-worktrees` carries one too and is reference-only, demoted in v1.18.0 for duplicating another plugin's description. **The seventh condition is not about size and was found by tripping over it.** A SKILL.md whose frontmatter has no closing `---` on its own line is **dropped from the vendored gate's census with no error at all** — breaking one file on purpose took the header from `98 SKILL.md (20 model-invocable)` to `97 SKILL.md (19 model-invocable)` and still exited 0. The cause was a script of this session's own that reassembled files from `'---\n' + fm + '\n---' + text[len(fm)+8:]` and glued the delimiter onto the body's first line in all 20; the gate now fails on it first, before any count computed over the wrong set. Also added: a two-line CI step asserting `within_budget` from `--json` (a new file, never an edit to the vendored script), and a documented **strict profile** for a model given only 6,000 chars of listing (rich ≤430, short ≤200, name-led ≤120, target 5,863). **The strict profile deliberately does not pass** — the shipped slate is 7,542, so it exits 1 and prints the exact gap of 19 descriptions. It is a target for a future pass, stated as a command rather than a memory. CI runs the default profile. Every failure path was confirmed by breaking something and watching the gate go red, then green again. No skill added, removed or renamed. Total: 98 skills.
- **v1.18.0** (2026-08-07) — **This repo's own descriptions were 23,174 chars, against an 8,000-char listing budget that every installed plugin shares. They are 7,542 now — so this repo has stopped dominating a shared budget. That is not the same as "its descriptions are now visible", and the difference matters.** The budget is GLOBAL: Claude Code sizes the whole listing at 1% of the context window (8,000 chars at 200k) and fills it from every model-invocable skill of every installed plugin, not per repo. On this machine that pool wants roughly a hundred thousand characters — **105 skills needing ≥91,094 chars**, established during this work by decompiling the harness; a filesystem walk over `~/.claude/plugins` on 2026-08-07 reaches 157 skills and 107,849 chars, counting cached duplicate versions the loader would not. Either way it is more than ten times the budget. **Admission is ranked by usage** — `usageCount × max(0.5^(days/7), 0.1)`, read out of `~/.claude.json → skillUsage` — and a skill that has never been invoked scores **0**. So whether any one description here reaches the model still depends on the global total and on this plugin's usage rank; 7,542 does not settle it, and no number this repo can measure alone would. Directly observed in a live session's listing: **20 agent-traffic-control entries appeared and 5 carried a description — the 5 with usage history.** That count moves with whatever else is installed and with what has been invoked lately (`~/.claude.json` held 8 usage records for this plugin on 2026-08-07, and none of them carries a `lastUsed` timestamp, so it cannot be dated or trended) — re-read it from a real session rather than quoting this line. **What this release DID achieve, and can be checked:** nothing is truncated mid-sentence at the per-skill cap, every description is materially shorter, and every skill is reachable. **What it did not:** any promise that a given description survives into the listing. **All 20 live descriptions are rewritten** into three size classes, chosen by whether the skill has a usage record — because that decides whether its description is certain to be read or merely might be: **rich ≤600 chars** (7 usage-backed skills, with `git-worktree` capped at 300), **short ≤280** (6), **name-led ≤160** (7). Total descriptions **22,221 → 6,599**. **`using-git-worktrees` is demoted to reference-only, and the reason first given for it was wrong.** It was not a bare name: `~/.claude.json` carries a `skillUsage` record for `agent-traffic-control:using-git-worktrees` (usageCount 1, re-derived 2026-08-07), so its description was being admitted and demoting it costs something real. The true reason to do it anyway is duplication: its description shares its **first 118 characters verbatim** with `superpowers:using-git-worktrees`, which is installed on the same machine and also usage-backed, so the listing was paying twice for one trigger surface and the model had no way to tell the two entries apart — while `git-worktree`, live and usage-backed, already covers that moment with a script. (The usage record carries no `lastUsed`, so it cannot be dated; it may date from this audit itself. The duplication argument does not depend on that.) It keeps 2 inbound links from live skills, so the route gate still passes. **`pre-dispatch-schema-probe` is renamed `inherited-scope-doc-names-may-not-exist`**: its bare name measured at *negative* separation, because "schema" and "probe" occur more in neighbouring skills' prompts than in its own — the name pointed at the wrong skill. **`git-worktree` is the one description that GREW**, 198 → 294, because 198 chars of "manages Git worktrees … following KISS principles" named nothing a user types; separation **+0.036 → +0.206**. Measured against the baseline committed in v1.17.0, on 13 committed eval suites: **8 of 13 clear their retention bar**; 3 more retain 100–411% of baseline separation and fail only the "negative mean must not rise" clause; 2 miss on retention — and for those a control measurement shows **no 280-char slice of the original text reaches the bar either** (`cross-worktree-spec-handoff-via-checkout-paths` 49% against a 45% best-available; `db-access-review-subagent-needs-explicit-probe-budget` 59% against 38%, one point short of the bar). `--compare` reports **0 NARROWED across all 21 touched files** — the silent class that tool exists for — and **57 quoted trigger phrases DROPPED**, which is the visible, deliberate cost of the cut. All 57 are itemised in the pull request, in a table generated by running `--compare --json` over every one of the 21 touched files and printing every phrase it returns — not hand-transcribed, because the first version of that table was and it silently omitted two skills and 6 of the 57 phrases. 0 over cap, 0 wrap corruption, 98 skills, one renamed, none added or removed.
- **v1.17.0** (2026-08-07) — **A measurement floor before anything gets rewritten: 11 new eval suites, 341 prompts, and a committed baseline.** The next release cuts 20 descriptions from 22,221 chars to under 6,700, and a cut that size is only defensible if there is a number to check it against. Two eval suites existed; there are 13 now, one for each skill in the two classes whose description does the selecting. **Each of the 11 NEW suites has 21 positive prompts and 10 negatives** — 231 positives and 110 negatives in all — the negatives lifted from the sibling skills a wrong pick would land on, so what is measured is *separation from close neighbours*, not recall. **The two pre-existing suites are not built to that shape and are not counted in any figure here**: `cross-worktree-spec-handoff-via-checkout-paths` carries 25 positives and 15 negatives, `pre-dispatch-schema-probe` 20 and 10, so all 13 together hold 276 positives and 135 negatives. **Every positive in the 11 new suites was written from the skill's BODY with the frontmatter unopened**, because a prompt written from the description it will later score measures whether the words survived rather than whether the trigger did; the mechanical form of that rule is *reject any positive sharing a four-word run with the current description*, and it caught **29 of those 231** — including four in `gh-issue-claim-coordination` that were verbatim quoted phrases out of its own description. All 29 were rephrased. **The rule was applied to the 11 new suites only.** Re-applying it to the committed files finds 0 offenders across the 231 new positives, against 8 of 25 in `cross-worktree-spec-handoff-via-checkout-paths` and 3 of 20 in `pre-dispatch-schema-probe` — those two are a different vintage and their separation figures are not comparable on this axis. `scripts/eval/baseline-2026-08-07.json` records the separation of all 13 at commit `6cbc557`; the spread is wide, from `cross-worktree-spec-handoff-via-checkout-paths` at **+0.534** down to `git-worktree` at **+0.036** — that last one is 198 chars saying nothing a user would type, and it is the only description the next release makes *longer*. One tool change: `score_trigger_coverage.py` refused a plain YAML scalar and exited, so `git-worktree` and `gh-issue-claim-coordination` **could not be scored at all** — the two most likely to rot. It now reads plain, quoted, folded and block scalars, mirroring `check_skill_descriptions.py::_scalar` so both gates read the identical string out of the same file. Its provenance note says exactly what diverged from upstream and why. No description changed in this release. Total: 98 skills.
- **v1.16.0** (2026-08-07) — **57 of the 77 reference-only skills could not be reached by any route, and now all 77 can.** Only a model-invocable skill is retrieved. The other 77 carry `disable-model-invocation: true`, so there are exactly two ways in: the user types the name, or a skill the model **has** retrieved names it in its body and the model opens the file. The Skill tool refuses a disabled skill outright — the message is *"cannot be used with Skill tool due to disable-model-invocation … Do not replicate this skill's workflow by other means"* — so the body-link route is the only one an agent can take on its own. Scanning every SKILL.md for mentions of every other skill's name, word-boundary matched, **57 reference-only skills were named by no live skill at all**: the entire `workflow-*` family, the async-post-commit-hook cluster, most of the squash-merge traps. The real retrieval surface was **41 of 98**, not 98 of 98. Each of the 57 now has a `[`name`](../name/SKILL.md)` link from the live skill that owns the nearest moment, under a new *Reference-only siblings in this toolkit* section — 13 live skills host between 2 and 9 links each. **Body text costs nothing in the skill listing**, so this was free: the listing total is 23,174 chars before and after. Also fixed **9 dead cross-plugin links** — four skills in `multi-agent-skill-silent-phase-compression` and one in `session-handoff-detect-prior-orphan-pr` were linked at `../<name>/SKILL.md` but ship in *other* plugins, so the link is dead on a plugin install (the same defect as v1.11.1); they are named in backticks now. New gate `scripts/check_skill_routes.py`, wired in as CI step 4, fails on an unreachable reference-only skill, a relative link that does not resolve, or a skill without exactly one README index row. Substring matching would have hidden one real orphan — it inflates `git-worktree` from 1 live inbound to 4 by counting mentions of `using-git-worktrees` — so the gate matches on word boundaries. No skill added, removed or renamed. Total: 98 skills.
- **v1.15.0** (2026-08-07) — **One new skill, four extended, from one routing-app session.** A triage pass over the then-97 skills concluded none of that session's six candidate learnings was new: four were a missing paragraph inside a skill that already fires on the failure, and two were already written down. **Review overturned that for one of the four; the rest of the triage stood.** **(1) NEW — `pre-dispatch-agent-isolation-parameter-not-prompt` (1.0.0).** The learning is *prose in a dispatch prompt is not configuration: isolation comes from the dispatch tool's `isolation: "worktree"` parameter and from nothing else.* It had been filed inside `concurrent-session-checkout-clobbers-shared-worktree`, and that is the one place it can never do any good: every trigger that skill has is a symptom of a collision that has ALREADY happened, and it carries `disable-model-invocation: true`, so it is one of the 77 skills that never appear in the model's listing at all — measured, not assumed: `check_skill_descriptions.py` reports 20 model-invocable of 97 before this PR, and the installed 1.12.0 plugin cache agrees. Advice that has to fire BEFORE a fan-out cannot live behind a post-damage trigger. The new skill is scoped to the pre-dispatch window only and deliberately does NOT re-describe the collision symptoms — three skills already cover those and a fourth would split the trigger surface. It carries the check (read the CALL back, not the prompt; `git rev-parse --show-toplevel` and `git branch --show-current` as every agent's FIRST bash call, echoed in its report, so two agents naming one directory is visible in 30 seconds) and the tell — a number that is TRUE of something but not of the branch you are measuring: `pytest docs` read **446** in the shared tree because another agent had committed a test; on the branch it was **445**. The worked case: four parallel implementation agents launched with *"You are in your OWN isolated git worktree"* in their prompts and the parameter unset; all four shared one checkout, one agent's first commit landed on a DIFFERENT agent's branch, and the ORCHESTRATING session's own branch was switched out from under it mid-run — none of it an error. `concurrent-session-checkout-clobbers-shared-worktree` (1.1.0 → 1.2.0) keeps a short pointer to it rather than a second copy of the words. **(2) `merge-conflict-generated-files` (1.0.0 → 1.1.0) — the broadened trigger is the load-bearing half.** It required "you can identify a single source generator", which is exactly why it never fired for a stated count in a README: there the derived thing is a VALUE and no generator exists. The variant: in a stack of PRs each adding tests and each editing the same counted line, the correct merged value is on NEITHER side — one side is the branch's base plus its own tests, the other is main plus everybody else's. Measured — **#780 resolved to 459 and the merged tree collects 461; #807 resolved to 461 and collects 476; #781 resolved to 476 and collects 480**: three resolutions, three wrong, each a real count of a real tree. Resolve to a literal `**PENDING-REMEASURE**`, finish the rebase, measure the final tree once, replace — a half-plausible number survives by inertia, because nobody re-checks a value that already looks like a value. And **the count can go DOWN**, so "bigger is newer" is not a tiebreak: a later PR rebased four times went **530 → 559 → 516** with the merged tree collecting **517**. **(3) `parallel-session-superseded-validated-fix-verify-live-prod-state-first` (1.0.0 → 1.1.0) gains the assessment variant.** The principle was already its headline, but every trigger bullet assumed you BUILT something ("invested a full implementation", "about to deploy"), so a session whose only deliverable is a judgement matched none of them. Three agents spent about **80 minutes** concluding **do NOT deploy** — main shrank every route and capped a 20 km wish at **12.16 km** — while a commit pinning the sizes landed mid-run; the verdict was false before it finished being written, and nothing in the analysis was wrong, only its base. Added: pin the base SHA and run `git log <base>..origin/main` before publishing the verdict; **an assessment older than its own runtime is not evidence**; put the re-check at the END of the fan-out, since the verifier that ran LAST is what caught it and a start-of-task check would have passed. Plus the measured danger window that makes a merge gate non-optional in a repo with no human reviewer — main carried the regression for **8h25m across 24 commits**. **(4) `pr-from-stale-branch-silently-reverts-newer-main-files` (1.1.0 → 1.2.0) gains needle PROVENANCE**, the half v1.14.0 left out: it prescribed a needle per claim and never said where the needle comes from. The #853 audit grepped `route_km_shortlist`, recalled from memory; the real symbol was **`shortlist_km`**, and for a minute the session believed a whole merged PR had been deleted. Added: source every needle from `git show <squash-sha> -- <file>` and paste the line beside the check; the two-sided failure, of which only one side is intuitive — a guessed needle that MISSES manufactures a phantom deletion, one that happens to MATCH manufactures a **false all-clear**, and nobody re-checks a clean audit; and current state cannot distinguish "never hit" from "hit and restored by someone else", so check `REF=<suspect-squash-sha>`, not `origin/main`, or another session's recovery PR reads as proof you were never affected. **(5) `pr-conflict-from-mid-flight-merges` (1.0.0 → 1.1.0) — the counted-line correction, put where it is actually retrieved.** That skill is model-invocable and is what a session reaches for when its PR conflicts after other PRs land, and its step 6 still demonstrated bumping a `342 tests` claim to `379` to match main. Copying main's number is right only when your own branch changed nothing the number counts; in a stack where every PR adds tests it is wrong every time. Step 6 now carries the neither-side rule, the placeholder-then-remeasure remedy, and the count-can-go-down caveat, and the worked example is marked as the special case it is. Also fixed: `CONTRIBUTING.md` stated the model-invocable split as "74 of 94" — the gate prints **77 disabled of 97** before this PR and **77 of 98** after it; and the victim-audit section invoked `./audit.sh`, a filename the skill never defined, now `sh needle-audit.sh` against the inline block above it, which the section now tells you to save under that name. **And the index above was one row short before this PR**: `executed-prompt-not-retired-sibling-redoes-it` shipped in v1.10.0 and was never listed — which is that skill's own failure, a file on disk with no row in the index that names it. Found by a link check that asserts every skill directory has exactly one README row; it now does, 98 of 98. One skill added, none removed, none renamed. Total: 98 skills.
- **v1.14.0** (2026-08-07) — *(entry backfilled in v1.15.0: the v1.14.0 commit bumped the three manifests and extended the skill, but never added its own line here.)* **`pr-from-stale-branch-silently-reverts-newer-main-files` gains the VICTIM side — check what you TOUCHED, not what you MADE.** v1.13.0 covered the #853 incident from the perpetrator's end; this is what the same incident cost at the other end. The natural check enumerates the things you *created*; the revert's scope is the things you *touched*, which is larger. Three ways that gap bit in one session, each after an earlier check had reported clean: in-place edits to records you did not create produce no new id, so an id/status/PR-number sweep passes while the prose underneath has reverted (one standing ruling was left publishing a superseded range with no marker); a PARTIAL rollback inside one record reads self-consistent, which is worse than a clean revert that at least looks old; and a MOVED number is indistinguishable from a reverted one by grep — a README count read 560 where the session had written 546 and both checks called it lost, when siblings had simply added tests, so v1.13.0's "diff the field, not just the key" produces that false positive as written. Re-derive the value; do not compare the string. Plus the executable form — a needle per CLAIM rather than per file, with a `REF` override so one script is both the pre-merge gate for your own PR and the post-merge audit for someone else's. Also fixed a red `main`: #16 had bumped `marketplace.json` and `plugin.json` to 1.13.0 and left `VERSION` at 1.12.0. No skills added or removed. Total: 97 skills.
- **v1.13.0** (2026-08-07) — **`pr-from-stale-branch-silently-reverts-newer-main-files` gains the variant that defeats its own fix.** The skill assumed the branch is *behind* main, so "am I behind?" catches it and "merge main in" repairs it. On 2026-08-07 a PR deleted 11 files and 15 hand-maintained ledger entries belonging to four other sessions — 59 files, 5,081 deletions — while being **fully up to date**: its parent was the newest commit on main, `merge-base --is-ancestor` was true, and `git merge origin/main` would have said *Already up to date*. Only the branch POINTER had moved onto main; the TREE was ~8 hours old, so everything merged in that window was recorded as a deletion. The tell is arithmetic — the branch differed from its own parent by 59 files but from an 8-hour-old commit by 39. Added: the `git diff --diff-filter=D` check that survives when every behindness check passes, the candidate-base comparison that identifies a stale tree, why merging main in is a no-op here, and a **splice-never-revert** recovery section (reverting the offending squash re-deletes everything merged since — the same failure aimed the other way), including that a ledger entry can be rolled back to earlier wording while the record still exists, which no id-presence check catches. Second worked example carries the two things that let it run: the PR was created and merged **11 seconds apart**, and it was found 3½ hours later by accident.
- **v1.12.0** (2026-08-06) — **`git-worktree` could not find its own manager script on a plugin install.** All 20 documented commands invoked `${CLAUDE_PLUGIN_ROOT}/skills/git-worktree/scripts/worktree-manager.sh` with no fallback of any kind. That variable is frequently unset in the shell a step actually runs in, so the path expanded to `/skills/git-worktree/scripts/worktree-manager.sh` and died with a bare `exit 127` naming no root at all — in a skill whose own **CRITICAL** heading says you must never call `git worktree add` directly. A reader hitting 127 has no way to tell a missing install from a missing path. A single resolver block near the top now covers all three roots and every command uses `"$WTM"`. Two details are load-bearing: the version segment sits at **`NF-4`**, not `NF-2`, because this script nests two levels deeper than a flat plugin layout, and ranking the whole path would sort by marketplace name instead — letting `aaa-mkt/2.5.0` lose to `zzz-mkt/1.0.0`; and the lookup uses `find`, not a shell glob, because zsh fails a non-matching glob at expansion time, before `2>/dev/null` can apply. Verified under bash, zsh and sh with `CLAUDE_PLUGIN_ROOT` unset: all three resolve the installed copy. This is the **executable** form of the bug class v1.11.1 fixed the documentation form of — that pass corrected six dead see-also links and did not sweep for executable lookups, so this exposure sat behind a changelog entry that read as though the class was closed. Same class as [`session-handoff#13`](https://github.com/wan-huiyan/session-handoff/pull/13). No skills added or removed; no descriptions changed. Total: 97 skills.
- **v1.11.1** (2026-08-06) — **Docs fix: six cross-skill "see also" pointers named a path that does not exist on a plugin install.** They were written as `~/.claude/skills/<other-skill>/SKILL.md`. That root is created by a manual copy-in install only. Install this bundle as a plugin and the skills live under `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/plugins/agent-traffic-control/skills/…` instead, so every one of those links pointed at a file the reader could not open. (`CLAUDE_PLUGIN_ROOT` does not rescue it either: it is often unset in the shell a step runs in, and it points at the plugin's own root, so it can never reach a sibling plugin.) These were see-also pointers, not executable lookups, so nothing ran and silently did nothing — the cost was a dead link. Since no single local path resolves under every install method, the fix names the skill plainly and adds a link that always resolves: a GitHub source URL for the four siblings that live in this same plugin (`gh-pr-merge-worktree-checkout-trap` ×2, `deploy-from-stale-worktree-silent-rollback`, `pr-conflict-from-mid-flight-merges`), the repo URL for `session-handoff` (a separate plugin — https://github.com/wan-huiyan/session-handoff), and the bare name for `gha-auto-deploy-never-ran-skipped-mask`, which is not published anywhere this repo can point at. A seventh occurrence in `multi-agent-skill-silent-phase-compression` was prose asserting that "every overnight-workflow skill in `~/.claude/skills/`" uses file-based state passing — not a link, but the same single-root assumption; it now cites the [overnight-workflows](https://github.com/wan-huiyan/overnight-workflows) repo instead. Files touched: `docs-branch-off-feature-branch-smuggles-code`, `gh-pr-merge-unstable-state-needs-auto-and-watch-branch-deletes`, `merged-pr-not-deployed-gate-label-missing`, `multi-agent-skill-silent-phase-compression`, `stacked-pr-base-branch-deletion-auto-closes-dependent`, `synthetic-id-collision-rebase` — each with a patch bump to its own frontmatter `version`. No skills added or removed; no skill descriptions changed. Total: 97 skills.
- **v1.9.1** (2026-08-05) — **Accuracy pass over v1.8.1 (rebased onto v1.9.0).** **(1) `pre-dispatch-schema-probe`'s description routed to a skill that does not exist.** Its closing clause read "Related but distinct from `verify-plan-constants-against-data`, which covers enum/category VALUES inside columns" — that skill exists nowhere in this repo or any installed bundle, so the redirect gave the model nothing to route to. The STRUCTURE-vs-CONTENT distinction is precision worth keeping and is kept; the dead name is out of the **description** — the text injected every turn — and the Notes section now states the boundary concretely instead. It stays in "See also", alongside other references to unpublished siblings: a sweep found **45** such dangling cross-references across this repo and `overnight-workflows`, so that is a repo-wide convention to settle deliberately, not one line to remove in passing. Description **1,465 → 1,423** chars (42 more headroom); `--compare` reports **0 dropped, 0 narrowed**, and the committed scorer reports 1 better / 19 same / 0 worse with separation **+0.4311 → +0.4366**. **(2) `CONTRIBUTING.md` promised more than the gate delivers.** It said the script "also fails on line-wrap corruption" without qualification, but the text report's exit code is built from `live = [s for s in skills if not s.disabled]` — and **74 of this repo's 94 skills are disabled**, so for the large majority a hyphen break fails nothing and is not even printed. Verified by injecting one into a disabled skill: text report exit 0 and silent, `--json` exit 1 and named. That is precisely why v1.8.1's four real corruptions had to be found via `--json` instead of CI — all four were in manual-only skills. CONTRIBUTING now states the scope and tells contributors to run the `--json` form too. **(3)** Gate re-vendored from `context-police@eedad0f` (v2.2.1) — its `find_wrap_corruption()` no longer reports a bogus `BROKEN BY LINE-WRAP` on `description: >-` skills (11 → 7 hits across `~/.claude/plugins/cache`, all 4 dropped being false positives). The cap arithmetic is unchanged from v2.2.0, so no char figure below moves. No skills added or removed by this pass. Total: 95 skills (v1.9.0 added one).
- **v1.9.0** (2026-08-05) — Added **`worktree-does-not-isolate-shared-installed-artefacts`**, the generalisation of `pytest-editable-install-resolves-to-primary-checkout-not-worktree` beyond Python. That skill had the right shape and could not be found: grepping all 94 for `simulator|xcodebuild|DerivedData|simctl` returned **nothing**, so an iOS session whose test results contradicted each other for an afternoon found no skill at all. A worktree isolates the SOURCE TREE and nothing else — a simulator install keyed by bundle id, a build cache keyed by project path, a tool cache keyed by version string and a dev server keyed by port are all shared by every worktree on the machine. Three properties make it expensive: the failure is CONFIDENT (detailed, plausible, wrong — not an error); detection-by-counting cannot see a stale *install*, because those cases do run; and the remedy is where the bug gets created, since copying and syncing are what you reach for to repair staleness. Worked example: one machine, ten worktrees, one iOS app, the same shape three times — `** TEST SUCCEEDED **` over zero executed cases, a simulator serving an app `xcodebuild` believed it had replaced (through a `clean` AND a `simctl erase`) that produced seven failing suites plus a layout guard reporting the exact values of a build reverted an hour earlier, and a tool cache whose repair reintroduced the defect being repaired. Also adds the missing warning to `prove-test-failures-pre-existing-via-clean-worktree`: its method is sound and was used successfully the same day (13 failures proved pre-existing, byte-identical sets), but its BASELINE is exposed to this pattern — one attempt silently ran test bundles built by two *other* worktrees, caught only because the failure messages carried their compiled-in source paths. Total: 95 skills.
- **v1.8.1** (2026-08-04) — Fixed two SKILL.md descriptions that exceeded Claude Code's per-skill listing cap (`skillListingMaxDescChars`, 1536) and were therefore being truncated mid-word in every turn's context: `cross-worktree-spec-handoff-via-checkout-paths` (2,146 → 1,501 chars; 611 chars, including the entire v1.3.0 cwd-relative-pathspec variant, had been past the cut since `f1254dd` shipped in v1.6.0) and `pre-dispatch-schema-probe` (1,539 → 1,465 chars; 4 chars past the cut since it was introduced in v1.8.0). Every distinct concept and every quoted trigger phrase survives the trim, verified with `--compare` (0 dropped, 0 narrowed) — what was compressed is version annotations, restated prose, and command detail the skill bodies still carry in full. Also repaired four hyphenated tokens that a previous line-wrap had split across folded-scalar lines in three manual-only skills (`task-framing-claims-need-subagent-grep-verify`, `workflow-parallel-fanout-omits-sequential-phases`, `subagent-driven-branch-ref-froze-stranded-commits`). New tooling: `scripts/check_skill_descriptions.py` (cap + wrap gate, vendored from [context-police](https://github.com/wan-huiyan/context-police) v2.2.0) now runs in CI and pre-push, `scripts/score_trigger_coverage.py` + `scripts/eval/*.eval-suite.json` make the trim's coverage numbers reproducible, and `validate_plugins.py` now cross-checks `marketplace.json`'s version against `plugin.json` (the gap that let v1.5.0 ship with a stale marketplace entry, and that let the same drift recur on this branch until review caught it). Caveat: being under the per-skill cap means *no longer truncated*, not *guaranteed visible* — `skillListingBudgetFraction` still collapses roughly 65% of this bundle's descriptions to bare names at 200k context. No skills added or removed. Total: 94 skills.

- **v1.8.0** (2026-07-08) — Added a sixth bucket, **F. Workflow-tool & multi-agent orchestration mechanics** (12 skills): schema/StructuredOutput agent traps (`workflow-schema-agent-retry-cap-oversized-payload`, `workflow-schema-agents-empty-loop-under-ratelimit`, `workflow-standalone-schema-agent-crash-and-args-string`, `cjk-structured-llm-output-truncates-json-needs-2x-tokens`, `pre-dispatch-schema-probe`), fan-out/pipeline plumbing (`workflow-pipeline-parallel-stage-returns-bare-array-dropped-by-collector`, `workflow-parallel-fanout-omits-sequential-phases`, `workflow-deterministic-compute-as-script-not-stall-prone-agent`, `workflow-large-markdown-deliverable-extract-from-output-json-not-retype`), and orchestration/deploy edges (`opus-ratelimit-fanout-retry-on-sonnet-throttled-waves`, `workflow-run-deploy-gate-fork-pr-ref-name-escalation`, `design-subagent-with-plan-schema-executes-and-deploys-live-infra`). Also sanitized pre-existing engagement-specific identifiers in 6 earlier skills (neutral placeholders). Total: 94 skills.

- **v1.7.0** (2026-07-08) — Added 20 skills harvested from parallel-session / multi-worktree work: 5 to **A** (live parallel-session coordination), 8 to **B** (worktree isolation), 2 to **C** (parallel-PR), 4 to **D** (subagent integrity), 1 to **E** (squash/merge). Every worked example anonymized to neutral placeholders. Each new skill keeps its authored invocation mode, so the bundle now sits at 63 manual-only (`disable-model-invocation: true`) and 19 auto-invocable — the same mixed policy the bundle already carried; a uniform pass can normalize it later if wanted. Total: 82 skills.

- **v1.6.0** (2026-07-08) — Refreshed 53 skills with their latest content (expanded worked examples, recovery steps, and sister-skill cross-references) and generalized every worked example to neutral placeholders (`the-project-repo`, `<analytics_pkg>`, `<user>`, `<org>`) so the toolkit carries methodology without engagement-specific details. Also fixed a version-drift bug where `marketplace.json` lagged the plugin at 1.4.0. Total: 62 skills.
- **v1.5.0** (2026-07-06) — Added `gh-pr-pickup-designated-branch-supersede` (E. Squash/merge mechanics): picking up a designated branch that supersedes an earlier PR. Total: 62 skills.
- **v1.4.0** (2026-06-01) — Added 4 skills (2 to D. Subagent integrity: `code-review-subagent-fabricates-specifics-to-inflate-severity`, `db-access-review-subagent-needs-explicit-probe-budget`; 2 to E. Squash/merge mechanics: `gh-pr-merge-squash-stdout-shows-sibling-files-as-created`, `solo-repo-branch-protection-stable-gate-and-self-merge`) and expanded `multi-agent-skill-silent-phase-compression` with section 7 (forcing-function terminal-output row for droppable late steps in single-agent long skills). Total: 61 skills.
- **v1.3.0** (2026-05-29) — Added 24 skills (1 to A, 8 to B, 5 to C, 7 to D, 3 to E) drawn from the parallel-session / worktree / subagent-orchestration lesson backlog, and refreshed 6 existing skills with expanded content (`gh-pr-merge-worktree-checkout-trap`, `stacked-pr-base-branch-deletion-auto-closes-dependent`, `stale-base-pr-silently-reverts-upstream-content`, `subagent-pre-existing-misattribution`, `synthetic-id-collision-rebase`, `pr-followup-commit-stranded-after-squash`). Total: 57 skills.
- **v1.2.0** (2026-05-13) — Renamed repo `agent-squad-hr` → `agent-traffic-control`. Added 10 new skills (2 to A, 4 to B, 2 to C, 1 to D, 1 to E). Dropped `pr-conflict-site-regen` as too project-specific (the reusable kernel is already covered by `merge-conflict-generated-files`). Refreshed 3 existing skills with latest content. Total: 33 skills.
- **v1.1.0** (2026-05-08) — Merged the original `agent-traffic-control` content (E. Squash/merge mechanics, 4 skills) into this single bundle. One-shot install reads better than two sister marketplaces.
- **v1.0.0** (2026-05-08) — Initial release: 20 skills covering A–D buckets.

## Related

- **[dashboard-audit-toolkit](https://github.com/wan-huiyan/dashboard-audit-toolkit)** — synchronous end-to-end audit for live data dashboards. Pairs naturally with this toolkit when you ship audit fix-bundles via parallel PRs.
- **[overnight-workflows](https://github.com/wan-huiyan/overnight-workflows)** — autonomous overnight loops for review polishing and insight discovery.
- **`superpowers`** (Anthropic) — the `dispatching-parallel-agents` skill there pairs naturally with `gh-issue-claim-coordination` here.

## License

MIT — see [LICENSE](LICENSE).
